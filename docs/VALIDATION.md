# Validation Status

## Visual-History Matched Pilot: 2026-09-13

Completed the authorized eight-run real-Luna pilot: Park A/B, Apartment B/A, Kitchen A/B, Furniture B/A; A current-only and B history-enabled, one attempt each, unchanged 180-second budget, enhanced rendering, high reasoning, adaptive exploration and no moving handoff. All source hashes and paired scenario/settings match; none changed during execution. All eight recordings are complete with zero dropped records, zero recorded contact episodes, no operator assistance and no false terminal completion claim. Reports were indexed and are visible in **Test results**. [Full protocol, metrics, trace findings and links](../.runtime/performance/history-ab-20260913/summary.md).

Current-only passed **2/4**, history **1/4**. Apartment passed in both (A 101.031 s / B 121.282 s to verified completion); only A completed the furniture circle (177.766 s). Park timed out in both. Kitchen A ended with an unknown/obstructed-trajectory error, while B timed out after stale-depth/no-progress interruptions. Furniture B identified the table correctly but its first circle hit stale depth, followed by reacquisition and unknown-path blocks, ending at 33/360 scored degrees. Therefore the single failed B furniture run cannot establish a recognition regression caused by history.

Total reported tokens: A **224,745**, B **361,660** (+60.9%); A sent 28 images, B 68 including 30 sheets and two original inspections. Request counts/paths differ, so this is not isolated per-image cost. Completion-time comparison is meaningful only for the jointly successful Apartment pair; early failures are not speedups. No overall improvement was observed in this pilot. One pair per challenge, uncontrolled cloud seed/underlying model identity, and 21 preserved idle/stopped simulator instances on a memory-constrained shared GPU limit inference. Recognition accuracy, repeated reliability and other challenges remain unmeasured. Existing default, prompts, safety gates and model weights were not changed. Prior implementation-only claims below remain historical.

## Saved Test Results Window: 2026-09-13

Added a **Test results** workspace link opening a separate read-only page at `/?view=test-results`. The page does not mount the observatory, create a robot WebSocket or send control requests. It supports search, evidence/scenario filters, progressive run browsing, trial outcomes and metrics, available final head-camera images, normalized JSON download, and explicit loading/error/retry/empty states. The responsive table scrolls within its own area on narrow screens. Legacy/incomplete/assisted results and missing fields remain distinct from verified passes.

The read-only backend discovers `.runtime/*/experiment.json`, one additional nested level and contained directories referenced by the performance index. It supports current case manifests and older challenge-list manifests, and can read per-case reports when a summary is not yet present. It returns only allowlisted fields; arbitrary source files, credentials, raw traces and replay HTML are not served. Image URLs use catalog IDs and integer trial indices, restricted to valid local PNGs. File reads are capped at 8 MB, discovery at 500 batches, trial display at 500 entries per batch; corrupt reports are counted and skipped. Local-origin guards remain. Actual archive inspection found **41 batches / 84 trials**, with zero unreadable batches; all current historical manifests predate schema-v2 tracking. Unit-test/Playwright logs and unsaved live sessions are outside this catalog.

Two focused backend tests passed in 8.75 seconds, covering old/current manifests, missing and partial reports, assisted outcomes, non-finite values, oversized reads, root/traversal restrictions, same-origin/image access and unchanged live robot state/connection count. Four enhanced-renderer browser workflows passed in 38.7 seconds: separate popup/opener isolation, no robot sockets or control requests, filters/details/download, refresh failure recovery, empty archive, 1440/768/390/320 layouts, and existing setup/manual flows. The first integration run found the new link competing with mobile Stop/reset header space; moving it into workspace navigation resolved the overflow without changing safety controls. Desktop/mobile results screenshots were visually inspected. Frontend build, Pylance syntax and edited-file diagnostics passed. No real-model calls or full-suite reruns were made; this is reporting UI evidence, not robot-task validation.

## Performance Tracking: 2026-09-13

The maintained [design/performance ledger](DESIGN_PERFORMANCE.md) now separates implementation checks from measured robot improvements and lists the visual-history study as not executed. The table-lap success predates history. New evaluator runs save schema-v2 experiment provenance, source/build/texture/challenge hashes, runtime/hardware identity, actual renderer, explicit evidence type, per-planned-case performance rows and an appended local index. The headline summary now retains missing planned attempts and requires complete unassisted recordings for success. An offline comparator writes JSON/Markdown per-challenge summaries, evidence labels and confound warnings; it never loads model configuration. Source drift during an experiment is flagged.

`--camera-history disabled` is an evaluator-only current-input ablation; normal browser continuous control remains history-enabled. Both conditions keep the same local processing and worker safety checks. This is not an exact historical-code baseline. Nine focused tracking/ablation/denominator/evaluator tests passed in 29.42 seconds (`.runtime/performance-tracking-20260913.xml`); three recording/comparison tests passed after final evidence-label alignment in 9.17 seconds. Scripted real-physics smoke runs exercised automatic report/index generation, and temporary synthetic comparison fixtures tested mismatch/missing-row rejection. These are infrastructure checks, not new robot benchmark results. No real Luna calls, weights, cloud settings or live sessions changed; full suites were not rerun.

## Motion-Camera History: 2026-09-13

Implemented automatic paired-camera history for continuous Luna control: a bounded recent ring, six diverse keyframes across a review interval, a chronological native-pixel 2x2 sheet, and guarded inspection of retained originals. Current images remain separate; one candidate-marked current frame replaces the prior raw/annotated duplication. No new camera renders, per-frame model calls, model weights, cloud resources or relaxed motion limits were introduced.

Four core tests verify a single 200 ms sampled visual change survives a 60-second review interval after eviction from the short ring; duplicate suppression; pose/visual diversity; bounded originals; current/future-frame exclusion; native-pixel sheet equality; age/episode rejection; and delayed acknowledgement without losing new views. A separate in-flight keyframe pool was added after a test exposed newer views being crowded out by older coverage. Unchanged frames during inference do not create another sheet. This synthetic visual-change result proves retention, not object recognition or guaranteed 5-Hz capture under load.

A real-worker lifecycle check verifies accepted paired frames outlive the eight-frame sensor cache, original retrieval, no private geometry in metadata, relocation invalidation and Stop rejection. A scripted supervisor test receives a sheet, requests the exact original, deliberately proposes movement from that original and verifies deferral/no current-position sighting, then obtains a fresh view; a cancellation-resistant late circle reply cannot move after Stop. No real Luna inference was used.

The affected backend run passed **23 of 25 checks** in 149.06 seconds (`.runtime/visual-history-20260913-regressions.xml`). Both failures were the large-flat moving-inference cases using the legacy CPU renderer, which stopped on the unchanged `SPATIAL_STALE` guard. A diagnostic process with history recording/selection disabled and monotonic sequence bookkeeping preserved reproduced the same stale-depth stop, so eliminating history computation did not resolve it. An earlier diagnostic bypass that failed to preserve monotonic sequence state was inconclusive and is not evidence. Both large-flat cases **passed with the live app's enhanced renderer** in 51.17 seconds (`.runtime/visual-history-20260913-enhanced-moving.xml`), including continued motion during inference, successful bounded handoff and late-reply Stop cancellation. The CPU-renderer limitation remains unresolved; no all-green full-suite claim is made.

Frontend build, edited-file diagnostics and Pylance syntax checks passed. The final enhanced-camera browser history/original/Stop workflow passed (13.5 seconds), and existing camera-selected navigation and live continuous-goal redirection passed (23.6/15.3 seconds). The browser checks exact 332x332 sheet and 160x120 original dimensions, actual trace image URLs, 1440/390/320 layouts and frozen physics after Stop. The sheet, original and mobile Trace screenshots were visually inspected. All browser inference is scripted; the full backend/browser suites and real-model accuracy/token-cost comparisons were not run.

Focused checks without model calls:

```powershell
./.runtime/env/python.exe -m pytest -q tests/test_feedback.py tests/test_navigation.py tests/test_worker.py -k "motion_camera_history or luna_motion_history or spatial_sampling_cost_and_manual_relocation_reset"
npm --prefix frontend run build
./.runtime/env/python.exe -c "import os,subprocess; raise SystemExit(subprocess.call(['npm.cmd','--prefix','frontend','test','--','--grep','motion history contact sheets'],env=dict(os.environ,MILO_RENDERER='enhanced')))"
```

## Scenario Preview Picker: 2026-09-13

The selected scenario now shows a compact real-scene thumbnail, concise task summary, category/difficulty, objective count and completion criterion. Full instructions/checklists stay collapsed. Selection is explicitly distinguished from the loaded episode, and furniture text follows the draft object/direction. Image loading reserves stable space; failures offer a retry without disabling scenario loading. Tablet fields wrap instead of shrinking, and phone selectors each occupy a full row.

Fifteen static 480 x 300 WebP assets, including the practice bench, were captured from actual enhanced spectator renders in the isolated port-8001 catalog test. Uniform background margins are removed with padding and preserved aspect ratio. All assets total 70,188 bytes (3,082-7,332 each); only the selected image is requested. They are operator-only starting-scene illustrations, independent of the live state and never sent to the model. Physics, scoring, controller behavior and model camera history are unchanged.

Final frontend build passed. The catalog capture/load/reset check passed; the three affected preview, furniture-selection and setup-lifecycle browser workflows passed after responsive fixes. Coverage checks all preview dimensions and nonblank pixels, descriptions, loaded/draft distinction, image error/retry, unchanged episode/robot goal/ticks/tokens during browsing, dynamic furniture criteria, and 1440/768/390/320 layouts with minimum control widths and no image/text overlap. Desktop/tablet/phone screenshots were visually inspected. An initial screenshot exposed squeezed phone controls that a document-overflow check missed; actual control-width checks caught the remaining tablet issue and both were fixed. The catalog test now awaits the reset HTTP response before verifying API/UI epoch, resolving a reset-poll timeout during generation. No production timeout changed. Full backend and browser suites were not rerun; no real model calls were made for this UI change.

To regenerate previews after scene changes, build first, run the opt-in capture below with port 8001 free, then build again to copy the generated assets into the served bundle. Normal test runs never rewrite previews. The test fixture owns its own simulator and does not reset live sessions.

```powershell
npm --prefix frontend run build
./.runtime/env/python.exe -c "import os,subprocess; raise SystemExit(subprocess.call(['npm.cmd','--prefix','frontend','test','--','--grep','predefined challenges load distinct'],env=dict(os.environ,MILO_RENDERER='enhanced',MILO_CAPTURE_PREVIEWS='1')))"
npm --prefix frontend run build
```

## Furniture Circuits

Added **Circle the Furniture**, with four selectable objects and two directions, depth-grounded continuous circling, reset persistence and independent physical lap scoring. No learned weights, model deployments or existing live sessions were changed. Speed and safety limits remain unchanged; "fast" is an optimization intent, not a scored minimum speed or a demonstrated speedup.

**Real-model development evidence:** the final enhanced-camera Luna trial identified the table in its first response, supplied its own image bounding box, and completed a clockwise lap. Physics verified completion at **132.547 seconds** from control start; total process-run wall time was 143.266 seconds, simulated time 80.5 seconds, actual travel 11.703 m and recorded contact episodes zero. One model response took 21.094 seconds; the local circle action took 102.781 seconds. Recording contains 2,012 samples with zero drops and no manual placements. Termination source is `physics`, not a model completion claim. The configured deployment name is `gpt-5.6-luna`; its underlying identity was not independently verified. Artifact: [final report](../.runtime/furniture-circuit-luna-directed-scan-20260913/summary.json), with trace and recording below the same directory.

Three earlier failed development runs remain retained: initial-view ambiguity/stale sensing (180-second limit), an uncaught unknown-arc rejection (35 seconds), and unchanged-heading rescans (150-second limit, 17/360 degrees). Directing the rescan toward the sensor-derived arc resolved the reproduced controller blockage. These runs span changed code/views/rendering and are not a frozen four-run benchmark. Only table/clockwise has a real-model success; sofa, chair, lamp, counterclockwise autonomy, repeatability and faster full-mission performance remain unqualified.

**Scripted tests, not model evidence:** all eight object/direction combinations completed through real physics with a known-geometry diagnostic follower. Separate scoring tests reject wrong direction/object, contacts, partial laps, jumps and same-time repositioning. A scripted image-box test using actual enhanced RGB-D and the production worker completed a full table lap with one directed rescan; an early-Stop variant completed the first 36-degree arc, then froze physics and discarded subsequent motion. These tests do not establish visual recognition. Unit tests also verify finite depth grounding, local path budgets and bounded same-target rescans.

Final frontend build passed. Sixteen focused backend regressions passed in 117.45 seconds, covering scoring/privacy/reset, cancellation/ownership and the existing visual-inspection completion mode; artifact `.runtime/furniture-circuit-final-regressions.xml`. The complete backend suite was not rerun. The full enhanced browser run passed 36 tests, skipped one incompatible spectator-only toggle check, and failed the catalog loop due to a reset synchronization race. Its test previously checked unchanged title/goal values before the new episode's WebSocket update; waiting for the rendered epoch fixed it, and both complete catalog-loop retries passed. No production behavior or timing allowance changed for that test repair. Furniture target/direction persistence, conditional controls, nonblank cameras/3D, real local motion and 1440/390/320 layouts passed in the broad run. Desktop and phone furniture screenshots were visually inspected. Browser tests use scripted inference and make no Foundry calls; the existing non-fatal Windows socket-teardown warning remains.

## Setup-First Progressive Disclosure: 2026-09-13

Scenario selection/loading now sits above the observation workspace. Goal/objective details, manual control and idle run settings use explicit disclosures. Missing Luna configuration is shown first; run tabs, counters and metrics appear only for an actual session. Active-run instruction drafts survive tab switches and redirects, and completed runs retain their trace without an inactive instruction composer. Local model controls appear only for selected or resident SmolVLA. On mobile, connection/goal setup precedes the viewports. Map destination selection requires a paired scan, and loading/busy states suppress conflicting actions while retaining Stop. Loading failures remain visible without clearing the goal draft.

Frontend-only change; no simulator, model, checkpoint, cloud configuration or actuator semantics were modified. Final build passed. The full enhanced-renderer browser suite passed **36 tests with one backend-specific skip in 5.6 minutes**. The skipped spectator-only graphics-toggle test does not apply when the unified enhanced backend owns graphics; the separate unified-graphics test passed. Earlier outdated expectations for hidden/manual/settings controls were updated to exercise the new journey rather than bypass its guards. No motion, camera-width, or Stop assertions were relaxed.

New coverage verifies setup order, connection save, scene-loading failure/retry availability, blocked competing starts, draft retention, active/completed run disclosures, manual-control access, Stop visibility and overflow at 1440/390/320 widths. Desktop and phone setup screenshots were visually inspected; their red error banner is intentionally injected by the failure-path test, not an unresolved application error. Existing scenario loading/reset, real-physics manual motion, continuous navigation, local-model residency, and goal-redirection workflows passed in the full suite. No real Luna/SmolVLA inference was used in these UI tests.

## Navigation Architecture Comparison: 2026-09-13

See [NAVIGATION_STUDY.md](NAVIGATION_STUDY.md) for the current architecture, exact policy input/frame contracts, hardware detection, pretrained NavDP/NoMaD integration and 45 primary closed-loop trials. Both learned checkpoints were strictly loaded and actually influenced wheel motion through the shared worker; no mocks are counted as model results. Conventional local goals were the strongest bounded baseline; obstacle detours, kitchen search and reliable learned return remain unsolved. The target RTX 5080 was not available: all model measurements used the RTX 3080 10 GB. Existing Luna/SmolVLA configuration, checkpoints, simulator and live sessions remain intact.

Final gates: 34 affected backend tests passed; 16 motion checks passed after adding a 0.2 m/s near-obstacle profile; frontend build and four enhanced browser workflows passed. Full suites were not rerun. The detailed report retains initial range/stale-sensing failures, failed rescan pilots, unsupported NoMaD point goals, modality/privileged-shield limitations, and the blocked fresh SmolVLA GPU manipulation comparison. One geometric physical pick/place diagnostic succeeded with known fixture geometry and no assisted attachment; it is not autonomous visual manipulation.

## Continuous Navigation Speed: 2026-09-13

Continuous local navigation now commands up to **0.5 m/s**, previously 0.35 m/s. This applies to the conventional camera-selected and Luna-selected route tracker, not legacy navigation, SmolVLA, arm motion or ROS. Acceleration (0.3 m/s squared), turning (0.5 rad/s), arrival tolerance, route budgets, sensor age limits, leases and Stop authority are unchanged. The tracker reduces requested speed using measured forward/diagonal range clearance and braking distance; the independent sensor and full-body safety checks remain authoritative. Existing running backends were not hot-patched.

Matched real-physics tests used the same 1.2 m local path at both caps on the parking and wider-post fixtures. Both fixtures completed in **6.10 simulated seconds at 0.35 m/s and 5.75 at 0.5 m/s**, with less than 0.04 m arrival error, no contact readings and no buffer stops. The commanded cruise cap increases 42.9%, but these short routes improve only 5.7% because acceleration and final approach dominate. Measured Bullet velocity peaks were 0.368/0.375 m/s at the old cap and 0.515 m/s at the new cap; command limits and acceleration were checked separately from physical tracking overshoot. These are scripted local-control results, not real-model mission results or wall-clock speedup measurements.

- **17 focused tests passed** (69.88 s), including both speed comparisons, pedestrian braking at 0.15/0.35/0.5 m/s, Stop, stale feedback, expired leases/deadlines, no progress and rejection of faster legacy/SmolVLA commands. Artifact: `.runtime/speed-20260913-focused.xml`.
- Initial integration run: **six passed, one failed** (109.58 s). The enhanced-camera route stopped safely on stale depth (1.156 s map age); the other checks covered replanning, narrow-path rotation, supervised Stop and pre-Stop camera rejection. Artifact: `.runtime/speed-20260913-integration.xml`.
- The unchanged isolated enhanced-camera route then **passed** (28.62 s): 1.219 m planned path, 7.453 s wall-clock route time, 65 updates, zero buffer stops, less than 0.04 m final error and 0.266 s maximum update gap. Artifact: `.runtime/speed-20260913-camera-recheck.xml`. No freshness allowance was increased; intermittent camera scheduling remains a limitation.
- Frontend build passed. The existing enhanced-backend camera-destination Playwright workflow passed (27.2 s test, 1.1 minutes including fixture startup), including 1440/390/320 layouts, changing live frames, arrival, no contacts, no model calls and no buffer stops. The existing Windows socket-teardown warning remained non-fatal. Earlier browser-launch attempts were interrupted or matched no tests and are not counted as validation.
- ROS now has its own unchanged 0.35 m/s cap instead of inheriting the continuous constant. Its contract rejects +/-0.5 m/s. Four of five ROS contract/ownership checks passed initially; takeover setup missed the existing 0.5 s sensor deadline, then passed unchanged in isolation (10.73 s). Artifacts: `.runtime/speed-20260913-ros-limits.xml` and `.runtime/speed-20260913-ros-recheck.xml`. This does not resolve or qualify the separate unfinished Nav2 integration.

The full backend/browser suites were not rerun. No policy weights, model installations, paid inference, simulator migration or increased physics time step were involved. Faster driving does not shorten model thinking, scanning or renderer stalls.

## Unified Enhanced Cameras: 2026-09-13

Newly launched apps now default to Enhanced for both the spectator and authoritative head cameras, including model inputs and paired spatial RGB/depth. Rendering uses the same Three.js material/light/color helpers, while PyBullet remains the sole physics authority. The backend setting overrides old browser quality preferences. Existing servers, model weights and datasets were not changed; historical standalone simulator/training scripts remain CPU-baseline by default, with explicit `MILO_RENDERER=enhanced` available for new enhanced runs. No new real-model trial or fine-tuning was performed.

The Node/headless Edge camera service is persistent and serialized. Request/frame identities and dimensions are checked; malformed output or timeout closes the renderer and stops motion. Resume refuses a failed renderer until reset. App resets reuse healthy GPU/map processes but drain prior jobs, reload the new scene and invalidate old camera URLs. Dedicated tests verify startup defaults, process reuse, shutdown, no TinyRenderer calls in enhanced capture, identical paired RGB at matching dimensions, bounded depth values, unchanged physics during capture, pre-Stop preview rejection, and API responsiveness after camera failure. The model-observation schema excludes render geometry and metadata.

Live integration exposed duplicate RGB rendering and map-thread scheduling delays. Enhanced spatial sensing now supplies its exact RGB to the live preview at 160x120; ordinary sensing-disabled preview remains 640x480. Map processing uses a pre-warmed separate process with one outstanding job and the original one-second age bound. Direct-action/policy previews are asynchronous. No sensor deadline, policy lease, speed or Stop assertion was relaxed. One final real-physics 1.2 m route arrived with 0.038 m error, 75 updates, zero buffer stops and a 235 ms maximum update gap in 8.937 seconds. This is a scripted local-route check, not full-task autonomy or a guaranteed frame/control rate.

Verification after integration:

- Frontend build passed; edited-file diagnostics were clear.
- Six enhanced-backend Playwright workflows passed in 1.7 minutes: shared world/head/depth graphics; desktop and mobile scripted model motion with changing camera/3D pixels; manual control and immediate Stop; expanded head camera; continuous camera-selected navigation. Responsive checks include 1440/390/320 widths. Log: `.runtime/enhanced-live-20260913-browser.log`. A Windows socket-teardown warning remained non-fatal.
- Three enhanced sensing/route/Stop tests passed after process warm-up (74.52 s). Both CPU/enhanced policy-camera thread checks and the Stop test passed with the short test trajectory (21.77 s); production policy limits were unchanged. Final renderer-fault/API-responsiveness test passed (12.77 s), and enhanced-default/reset/reuse/shutdown API test passed (23.89 s).
- Earlier affected-backend run: 149 passed, four failed in 569.91 s, XML `.runtime/enhanced-live-20260913-backend.xml`. The enhanced stale-map failure led to process isolation; the three CPU timing/lifecycle failures passed unchanged in a seven-test isolated recheck (33.90 s). That broad run preceded the final resource-reuse/fault changes; no single all-green broad rerun is claimed.
- Final standard/CPU Playwright suite: 33 passed, one enhanced-only test skipped, two failed in 7.5 minutes. Manual-controls setup socket failure passed unchanged on recheck; CPU continuous navigation remained unsuccessful (stale depth in the full run, legacy snapshot-process startup timeout on retry). The equivalent enhanced workflow passed. This CPU-baseline issue remains unresolved, so the full application regression is not entirely green.

Earlier multi-test enhanced attempts caught costly per-reset process starts and a test leaking its enabled spatial preference into later 640-pixel camera assertions. Persistent resource reuse and explicit test cleanup resolved those failures without changing camera-resolution assertions. Enhanced test-server startup has a 90-second warm-up allowance; in-test control timeouts are unchanged. New appearance can change RGB-based floor segmentation and learned-policy behavior; neither broad semantic accuracy nor local-model GPU co-residency has been qualified.

## ROS 2 / Nav2 Foundation: 2026-09-13

**Experimental and incomplete. Do not treat this as a working autonomous navigation mode.** The opt-in bridge keeps PyBullet and the validated worker authoritative. Existing Luna and SmolVLA control paths remain available; no learned weights, cloud resources or hosted agents were changed.

- Built the local `milo-nav2:jazzy` Docker image from `ros/Dockerfile`, including Nav2 1.3.13 and SLAM Toolbox 2.8.5. The source uses a distribution tag, not immutable dependency pins.
- `ros/milo.launch.py` uses SLAM Toolbox's lifecycle-aware launch and installed Nav2 defaults with Milo overrides. Actual container checks confirmed active navigation, 720-ray LaserScan delivery, depth PointCloud2 delivery and a sensor-derived SLAM map containing the robot origin.
- Karto's map bounds exclude rays above its usable threshold; its occupancy update also ignores readings at the sensor maximum. The bridge therefore caps readings at the exactly representable 7.875 m usable threshold, below the 8 m sensor maximum. These threshold endpoints extend the map without being obstacle endpoints. Self-occluded laser rays remain unknown. Nav2 obstacle marking is limited to 7.5 m.
- Sensors contain paired 160x120 head RGB/depth, calibrated optical transforms, wheel odometry, current robot footprint and synthetic planar LiDAR. They do not export scene objects, world pose or evaluator goals. Flat-ground sensor transforms and broad occlusion/geometry coverage still need qualification. The existing simulator-privileged clearance shield remains in use; this is not a purely sensor-based safety system.
- `MILO_ROS_ENABLED=1` enables `/api/ros/sensors`, `/api/ros/start` and `/api/ros/velocity`. Motion requires a connected operator, exclusive worker ownership and a new session-bound local odometry target 0.15-1.8 m away. Existing 60 s / 2 m local skill limits remain. Commands must be ordered, refer to a sensor packet at most 0.5 s old and renew the 0.5 s watchdog. Stop, takeover, reset and disconnect invalidate pending motion.
- ROS currently uses wall time, with capture-time timestamps. Reported simulated time is published separately on `simulation_clock`, not `/clock`. No hard real-time cadence is established. A bridge exit ends the ROS launch; a new local goal currently requires restarting that launch.
- Five backend ROS guard/API cases passed after direct JSON serialization and paired-preview reuse (17.90 s). A separate real-physics stream regression passed (11.04 s), accepting 15 command exchanges while forbidding redundant preview rendering. These are scripted transport/control checks, not Nav2 mission successes.
- **Three live 0.6 m goal attempts failed** with `ROS_WATCHDOG`, one accepted zero-velocity command each and zero recorded travel. Early attempts also exposed map bounds that excluded the start. After the map correction, Nav2 began planning, but Collision Monitor requested transforms newer than the available sensor data and command renewal still missed the watchdog. Safety deadlines were not extended. The ROS test container is stopped.

Next gate: restructure the serialized sensor/command exchange and qualify timestamp alignment, sustained local-goal motion, sensor loss and late cancellation. Then test obstacle departure/clearing/resumption and full-footprint stationary parking. Frontend ROS controls, Luna goal delegation, full-mission sequencing and full regression gates are not complete. No NavDP/NoMaD policy was installed or evaluated.

## Graphics Snapshot Pilot: 2026-09-13

The optional Enhanced spectator and a separate offline Three.js RGB/depth renderer share physical materials and lighting. TinyRenderer remains the live sensor and training baseline. No policy weights, controller, physics limits, scene colliders or model observation schema were changed. This is a rendering pilot, not a completed engine migration or a trained-policy improvement.

The larger benchmark passed **20/20 rendering cases**, ten snapshots across two scene types/five head poses and two resolutions, each tested with Standard and Enhanced Three.js. Ten measured captures per mode/snapshot followed an excluded warm-up. The process reported actual **ANGLE / NVIDIA RTX 3080 / D3D11**, not software fallback. Minimum valid-depth-mask IoU was **0.99349**; worst interior-surface p95 depth difference from TinyRenderer was **0.00651 m**. The acceptance gate excludes depth discontinuities and requires more than 5% interior coverage; these figures do not establish pixel-perfect edge agreement. All RGB outputs passed dimensions and sampled-color checks.

| Kitchen Views | CPU Capture Median Range | Enhanced GPU Local Median Range | Enhanced GPU Round-Trip Median Range |
| --- | ---: | ---: | ---: |
| 320x240 | 74-100 ms | 30-31 ms | 85-90 ms |
| 640x480 | 298-370 ms | 88-99 ms | 184-188 ms |

Ranges are across three head poses, not confidence intervals. Both paths produce RGB PNG and axial depth; GPU round trip includes browser IPC/base64 transfer. GPU-local timing includes drawing, readback, PNG encoding and depth processing; it is not shader-only GPU time. Neither measurement includes process startup or scene export/loading, and the CPU/GPU loops were sequential on a shared workstation. No sustained live control frequency or general performance superiority is claimed.

The requested local-policy contention check was **skipped**: only **2,445 MiB** of GPU memory was free before model loading, below the benchmark's 4,000 MiB guard. Existing workloads were left untouched. One renderer-time whole-device sample was 7,655/10,240 MiB used; this is neither renderer-only allocation nor peak VRAM. No new policy inference or training ran, so co-resident performance remains unverified.

Validation: **39 affected backend tests passed in 73.78 seconds**, covering simulator/camera boundaries, robot assets, recording and the depth-agreement gate. Both new browser tests passed in 44.6 seconds including fixture startup, with real physics and no cloud calls: responsive 1440/390/320 layouts, nonblank WebGL, unchanged sensor frames on quality switching, head motion, loaded textures, deterministic same-seed RGB, changed lighting without changed depth, resize, and wrong-episode rejection. An initial test-only timeout from thousands of per-pixel assertions was fixed by aggregating those checks without relaxing the timeout. The frontend build and edited-file diagnostics passed.

The full browser suite passed **33/34 in 6.9 minutes**. The continuous camera-destination test stopped as `SPATIAL_STALE` (438 ms maximum tracker update gap in the full-run snapshot) instead of arriving. An unchanged isolated retry also failed on stale depth. This remains unresolved; the safety threshold and test assertions were not relaxed, and causality has not been attributed to the graphics change. The complete backend suite was not rerun. The graphics pilot is therefore not an all-green application release.

Artifacts: [.runtime/rendering-qualified-20260913/report.json](../.runtime/rendering-qualified-20260913/report.json), [.runtime/rendering-qualified-20260913/comparison.png](../.runtime/rendering-qualified-20260913/comparison.png), and the earlier `.runtime/rendering-probe-20260913` smoke run. Desktop/mobile screenshots were saved and programmatic pixels/layout checks passed; the image tool returned resource references without a reliably viewable image, so subjective visual inspection is not established. Isaac Sim/Lab were researched only; neither was installed or benchmarked.

## Five-Room Kitchen Search: 2026-09-13

Added the separate `flat_kitchen` / **Find the Kitchen** preset: a 12 x 10 m floor, five furnished rooms, 2.1 m central hallway, five 1.6 m hall doorways, and a second living-room/bedroom passage. Detailed static furniture and shared kitchen/bathroom fixtures render in both cameras. The kitchen is hidden from the initial head view. Geometry, room names and arrival bounds remain outside model observations; the public goal asks for minimum elapsed control time without requiring every room to be visited.

The existing ordered-objective scorer verifies the base/wheels fully inside the kitchen's private clear interior, floor contact, rest and 0.5 simulated seconds of dwell. It rejects other rooms, threshold-only entry, movement and airborne poses, and clears completion on departure. Room descriptions and globally shortest-path claims are not graded. The browser defaults this new preset to the existing custom continuous controller with moving proposals enabled; it does not start inference on load, load SmolVLA, or connect to ROS/Nav2. The shared prompt's destination-memory instruction now follows the user goal instead of hardcoding the bathroom.

Initial verification: **four focused backend tests passed in 68.63 s**, including real wheel travel through every room and the alternate route, private sensor inputs, initial occlusion, open doorways and arrival conditions. **Three focused browser tests passed in 53.4 s** after separating immediate Stop-latch assertions from asynchronous controller cleanup. They cover catalog/load/reset, desktop/mobile rendering, real movement, 1440/390/320 px enhanced-mode captures, default Start settings, unloaded SmolVLA and a frozen simulation clock during Stop cleanup. The frontend build passed with the existing large-bundle warning.

Scripted moving-supervisor coverage now includes this flat as well as `park`. Early flat trials intermittently failed to advance another 4 cm within the existing 3-second window. Diagnostic telemetry identified `SPATIAL_STALE`, with the depth map crossing its unchanged one-second freshness limit during moving-proposal work. Profiling identified recursive copying of the superseded depth observation and Python floor-polygon conversion overhead; moving proposals also captured paired RGB/depth then discarded RGB and performed another blocking render.

Spatial processing now copies independent mutable map arrays without copying the replaced observation, batches polygon-coordinate conversion, and culls wholly off-map polygons. Moving proposals use the exact 160x120 RGB from their depth capture with a corresponding AgentObservation and bounded frame cache; they no longer perform a second full RGB render. Normal stopped feedback and the operator preview retain their existing resolutions. Exact raster equivalence is tested for 2,005 individual random/edge/degenerate polygons and their union; map-copy tests verify equality and no shared mutable arrays. No geometry, freshness threshold, motion budget, safety check or action deadline was relaxed.

After these changes, **12 focused sensing/handoff tests passed in 103.33 s**, including both flat moving-inference variants, uninterrupted measured motion during the pending reply, a successful continuation, and Stop/revision/arrival invalidation. Broader affected regression: **114 passed, one failed in 554.19 s**. The remaining pre-existing `pedestrian_crossing` floor-region final-parking test ends in bounded recovery without completion; it was already unresolved before this scenario and was not modified to pass. XML: `.runtime/flat-kitchen-20260913-regression.xml`. The complete backend suite was not run. Final strengthened raster/copy tests: **2 passed in 3.85 s**.

**Full browser suite: 35 passed in 4.5 minutes**, using isolated real physics and scripted inference, with existing Windows socket-close warnings. Pylance syntax and editor diagnostics are clean. The separate live preview at `http://127.0.0.1:8014/?graphics=enhanced` has the new flat loaded, zero simulated time, no active inference, and SmolVLA unloaded. Live 1440/390/320 px checks found nonblank canvas pixels, 640px head images, no horizontal overflow and no page errors. Desktop screenshot inspection confirms five distinct furnished rooms and open passages. Existing previews were left untouched.

No real-Luna full-flat run or minimum-time benchmark has been performed. These checks establish the scenario and bounded local-motion mechanics, not globally shortest exploration, reliable semantic kitchen recognition, or uninterrupted whole-mission driving. The older 30-trial table below predates this new preset.

## Scenario Overview: 2026-09-13

The goal remains smooth, freely directed motion from natural-language commands. Short continuous routes and interruption handling work in scripted real-physics tests. Complete autonomous missions remain uneven. All **30 planned real-model trials are complete**: 18 navigation baseline trials, six manipulation trials, and six focused refined trials. Every scored scenario has two trials, and the unscored bench has a separate scripted motion smoke test. Video coverage is complete with 17 verified Desktop clips.

Each real-model trial uses a fresh worker, the preset's goal and start, high reasoning, and a 300-second time-only control budget. Navigation uses `luna_continuous`; manipulation uses `single_step`, which exposes the existing validated arm/gripper tools. No trial uses SmolVLA, privileged goal coordinates in model inputs, or operator placement. The deployment is configured as `gpt-5.6-luna`; its underlying model identity and cloud seed are not independently verified. Two repetitions at development starts are diagnostic evidence, not a reliability estimate on unseen environments.

| Scenario | Baseline Final Physics Pass | Refined Final Physics Pass | What Still Needs Work |
| --- | ---: | ---: | --- |
| Park | 0/2 | Not rerun | One trial briefly completed, then left the bay; completion preservation and recognition. |
| Local Model Parking | 2/2 | 1/2 | Refined repeat 01 left a transiently valid pose and falsely confirmed parking; repeat 00 spent 103.8 s in arrival inspection. |
| Apartment Search | 0/2 | 2/2 | New stationary inspection completed in 95.5/146.4 s; generalization beyond this start remains untested. |
| Kitchen to Bathroom | 0/2 | Not rerun | Both timed out after about 6 m; correct room selection and full entry remain unreliable. |
| Remember and Recharge | 0/2 | Not rerun | 9-13 m of travel without a completed objective; mission sequencing and docking. |
| Clinic Delivery | 0/2 | Not rerun | One early static-clearance stop; the other timed out without arrival. |
| Warehouse | 0/2 | Not rerun | About 10 m each without a completed objective; semantic route progress and arrival. |
| Inspection | 0/2 | Not rerun | Both timed out after 7.9/12.4 m; the new viewing mode has not been evaluated here. |
| Pedestrian Crossing | 0/2 | 0/2 | Yield and full pedestrian passage improved from 0/2 to 2/2; continuation to parking still fails. |
| Tidy | 0/2 | Not applicable | Neither lifted the cube; 46/61 and 36/56 tool calls adjusted the head. |
| Sort | 0/2 | Not applicable | Neither lifted a cube; invalid/unreachable requests and repeated head adjustments. Repeat 01 recorded one contact episode. |
| Workshop | 0/2 | Not applicable | No lift despite 27 end-effector calls per trial; four collision-blocked requests across both trials. |

All 30 recordings are complete with zero drops. The 24 navigation recordings have zero sampled contact episodes; the six manipulation recordings have one contact episode, in Sort repeat 01. Navigation baseline and manipulation trials have zero false terminal claims; the refined comparison has one (Local Model Parking repeat 01). A safety stop, intermediate objective, transient success, and final physical success are reported separately. The refined parking failure used the ordinary parking checks for its final confirmation, so the added visual mode is not the sole explanation. It did incorrectly choose visual inspection earlier; completion-mode selection also remains fallible.

All six manipulation trials reached their 300-second control budgets without a scored lift. Tidy and Sort repeatedly adjusted the head; Workshop instead attempted many end-effector/gripper actions without securing a lift. Inference accounted for roughly 243-252 seconds per trial. This identifies a perception-to-grasp and action-sequencing gap in direct Luna tool control; it is not a test of the fine-tuned arm policy. Rejected collision or unreachable requests are not successful grasps. The single sampled Sort contact is retained and has not been classified as intended gripper contact versus unintended contact.

Priorities supported by these results: preserve valid arrival poses; distinguish obstacle clearance from full destination containment; reduce repeated parking sweeps; clear obsolete moving-obstacle evidence only from fresh sensor observations; improve goal-directed room/route progress; establish sensor-grounded grasp acquisition and bounded recovery from repeated visual searching. Do not feed the evaluator's hidden bay or target coordinates into the controller to make these tests pass. The learned-control research baseline and retained checkpoints remain unchanged.

Artifacts: `.runtime/comprehensive-20260913-parking`, `.runtime/comprehensive-20260913-navigation`, `.runtime/refinement-20260913-navigation`, and `.runtime/comprehensive-20260913-manipulation`. Per-case reports, traces, paired images, recordings and source-hash manifests preserve each version. The parking baseline predates the pedestrian guard update; this is not a single frozen-version 30-trial benchmark. The existing manifest does not hash `simulation.py`, a reproducibility limitation. No existing live session was restarted.

## Bounded Waiting And Clearance Feedback: 2026-09-13

Following the scenario baseline, continuous supervision gained a `wait` decision for 0.5-4 simulated seconds, executed in validated stationary segments of at most half a second. Cancellation is checked around feedback and execution, and intentional waits are not replaced by adaptive no-progress exploration. Luna-selected continuous routes now use the existing clearance-pause mechanism: a clearance stop clears motion and ends the route as blocked before refill, allowing fresh stopped feedback and a new model decision. Contacts, other runtime failures and explicit Stop retain their existing behavior. Manual camera-selected routes retain fail-stop behavior. No map clearing, background world stepping, motion-limit change or evaluator-state input was added.

Four focused tests passed in 37.44 seconds: three real-physics stationary-wait cases (normal, Stop during feedback, Stop during ticks), and a scripted two-decision supervisor approach/yield test. The latter selects an observation-derived floor candidate, receives the moving-obstacle pause, waits four simulated seconds and completes the two yield/crossing objectives without contact. It does not test final parking or real-model decision quality. The broader navigation/lifecycle regression then passed **180 tests with zero failures in 668.549 seconds**; XML: `.runtime/refinement-20260913-control.xml`.

Object-viewing goals additionally support `completion_mode=visual_inspection`: the controller holds the current view for one simulated second before requesting fresh model confirmation, instead of rotating through a room-parking panorama. The default `parking` mode retains perimeter, surface and margin checks. Executed actions and parking inspections invalidate the visual-inspection flag. Both modes report model completion as an agent claim, never as independent physics success. A deliberately false scripted inspection claim remains uncompleted under the scorer. That new workflow and both existing observed-candidate/Stop variants passed **three tests in 58.81 seconds** after the final change. The 180-test run preceded this completion-mode addition. A subsequent late-reply test confirms that Stop during post-clearance inference prevents a returned wait from advancing physics; both crossing variants and visual inspection passed **three tests in 50.09 seconds**.

The refined full browser run passed **31 of 32 tests in 5.2 minutes**, including continuous physical navigation and both instruction-redirect modes. The remaining camera-dialog assertion compared a live frame with an earlier captured frame ID. Comparing both views atomically in one browser snapshot fixed that test race; **three repeated camera checks passed in 34.2 seconds**, preserving responsive layout, live-image and Stop assertions. No production camera behavior or timeout was changed.

The six-trial real-model comparison is complete: **Apartment Search 2/2**, **Local Model Parking 1/2**, **Pedestrian Crossing 0/2 final parking, 2/2 yielding and full pedestrian passage**. All six recordings are complete with zero sampled contact episodes. One parking completion claim was false. Apartment control durations were 95.485/146.36 seconds; the successful parking run ended at 174.359 seconds. Both crossings exhausted the 300-second budget after earning two of three objectives. The table above retains the failures and compares the corresponding baselines.

## Pedestrian Crossing And Scenario Assessment: 2026-09-13

The new Pedestrian Crossing preset exercises an approaching robot, a moving person, yielding, and parking beyond a crosswalk. The original static lookahead stopped the robot before initial contact but allowed the crossing person to hit its projecting arm during a subsequent hold. The buffered-navigation shield now predicts measured obstacle translation as well as robot motion. Its 0.8-second horizon and whole-body margin prevented contact throughout the scripted crossing in both fail-stop and feedback-pause modes. Motion limits and explicit Stop authority are unchanged.

Four focused physics/catalog checks passed in 16.58 seconds, including the complete crossing, fresh scripted continuation to physical parking, Stop rejection, per-tick contact-failure latching, reset and observation isolation. The recorded scripted worker demonstration completed all three objectives with zero sampled contact episodes and a false per-tick pedestrian-contact latch. This demonstrates a bounded simulation safety case, not learned yielding or certified human detection. The shield uses privileged simulator geometry and velocities; direct manual/single-step motion does not use this predictor. Idle inference and Stop freeze the world. This initial baseline lacked autonomous waiting; the refinement above adds it. Dynamic-map clearing remains missing.

The first baseline real-Luna crossing trial selected forward travel toward the green bay and was stopped by `CLEARANCE_STOP: Moving obstacle approaches the robot's predicted swept volume.` after 0.371 m measured travel. It recorded zero contact episodes, but completed zero objectives and ended after 31.047 control seconds. That is a successful safety intervention and an unsuccessful autonomous mission, not a full yield-and-resume pass. The completed assessment and the later yielding refinement are reported above.

### Regression Evidence

- Full backend run: **408 passed, one failed**, 409 total, 1081.067 seconds. The only failure was the API test's outdated expected catalog, which omitted the newly added scenario. After updating that assertion, the catalog/reset test plus all recording tests passed: **12 passed in 25.44 seconds**. The complete suite was not rerun after that test-only correction. XML: `.runtime/comprehensive-20260913-crossing-backend.xml`.
- Frontend build passed with the existing bundle-size warning. Both advanced-ground browser checks passed at desktop/mobile widths, including the new pedestrian catalog entry and nonblank spectator/head-camera rendering.
- Full scripted browser run: **31 passed, one failed** in 5.1 minutes. The continuous-route test stopped with `SPATIAL_STALE` under concurrent load. An unchanged isolated rerun reached the destination within 4 cm, with 153 updates, no contacts and zero buffer stops, but exhausted the total 45-second harness budget during viewport checks. With a 90-second harness ceiling it passed in 42.6 seconds, including 1440/390/320 px layouts. The 30-second arrival assertion and all production freshness/motion deadlines remained unchanged. Stale-depth stops under load remain a responsiveness limitation.
- Browser inference is scripted. Real-Luna reports and the separately labeled scripted crossing recording are different evidence categories. Existing live sessions and loaded policy processes were not restarted.

### Video Evidence

Desktop exports are in `Milo Scenario Tests 2026-09-13`. They use recorded head-camera samples and measured physical routes, with explicit real-model/scripted labels, wall and simulated clocks, playback speed and final physics result. The scripted crossing additionally includes recorded evaluator-only spectator frames. Real-model clips use the first trial, including failures, at 4x; the scripted crossing plays at 1x. They contain no audio, and encoded 10 fps does not imply native 10 fps camera capture. The exporter validates frame ordering and media paths; decoding checks verify dimensions, frame counts and nonblank/changing camera samples. Per-video JSON preserves provenance. All **17 MP4s passed full decoding checks**: one for every preset including the unscored bench, an extra scripted crossing, and three separately named refined clips. The Desktop text index explains the results and provenance. Automated image checks are verified; the editor image tool's resource-only response did not provide a dependable new visual review.

## Adaptive Navigation And Goal Conditioning: 2026-09-13

Continuous Luna navigation now defaults to adaptive exploration: the existing arm preparation and four-pose floor scan remain, while two routine full startup rotations are omitted. Subsequent scan decisions select a missing nearby heading (at most 180 degrees) using views no older than 90 seconds within 0.25 m. Arrival inspection remains a full downward sweep. Settings retain an explicit legacy-scan comparison option.

After four completed actions without reaching a novel measured endpoint, the stopped supervisor can select a currently reachable unvisited candidate, a missing view, or a currently revalidated candidate near a previous route start. Two recovery attempts are allowed before holding with an unachievable outcome; novel translation resets that allowance. Recent failed targets are excluded. Controller interventions are labeled separately from Luna replies. The memory and UI expose progress counters, and Luna receives recent executed-odometry endpoint associations with unverified place sightings. These associations are not room boundaries, verified doorways, curved paths or current clearance guarantees.

### Real-Luna Comparison

Six fresh isolated `luna_continuous` workers ran kitchen-v1 case 0 twice per condition, high reasoning, 180-second time-only budgets. Saved source hashes match across conditions. Legacy is an ablation in the current implementation, not an exact pre-change snapshot. Artifacts: `.runtime/adaptive-navigation-20260913-{legacy,adaptive,handoff}`.

| Condition | Startup Preparation, Both Runs | Physical Completion | Recoveries, Both Runs |
| --- | --- | ---: | --- |
| Legacy scans | 53.172 / 54.516 s | 0/2 | 0 / 0 |
| Adaptive scans | 12.016 / 11.234 s | 0/2 | 1 / 0 |
| Adaptive + moving proposals | 11.625 / 11.906 s | 0/2 | 1 / 1 |

Median startup preparation fell from **53.844 to 11.625 seconds (78.4%)** in the legacy/adaptive comparison. All six runs reached the time limit without physical completion or an unverified completion claim. Recordings were complete with zero drops and zero recorded contact episodes. No mission-success improvement is established by this small fixed development-start comparison; cloud sampling and underlying Luna identity were not independently controlled/verified.

The moving-proposal trials recorded zero accepted retargets and deferred four/three decisions until fresh stopped feedback. Both recorded zero buffer-starvation stops; stationary inference was 42.469/42.561 seconds. A lingering `thinking` phase can include waiting for route arrival, so phase-tagged motion time is not a valid measurement of actual inference overlap. Existing delayed-inference scripted tests establish overlap and accepted handoff mechanics, not a real-model performance gain. `continuous_routes` counts feedback records and can include running snapshots; it is not a reliable unique-route or arrival total with moving proposals. Maximum recording gaps were 6.234-8.14 seconds across conditions, mainly startup; zero drops does not imply uniformly dense sampling. Action timing includes settling and may overlap inference, so timing categories must not be summed as disjoint phases.

### Real SmolVLA Comparison

Added an offline `--conditioning waypoint` docking adapter. It acquires the green marker from paired head RGB through the existing calibrated projection, retains that goal in wheel odometry, and recomputes robot-relative forward/left text each prediction. Learned velocity outputs pass through unchanged before the common clipping/deadband. No evaluator coordinates enter policy requests. This adapter is experimental and does not replace the live five-primitive interface.

With the same preexisting task-waypoints-12500 checkpoint, seed 716, six frozen docking-v1 cases, one-second stop-ended buffers and unchanged 60-second limit, **waypoint conditioning passed 4/6 versus task-only 0/6**. Passing waypoint cases 0/1/2/5 took 37.875/56.093/52.812/47.047 seconds; cases 3/4 timed out. Every failure remains in the denominator. Both manifests identify weights SHA-256 `bc30a9f4bd700e8fb71f0eb8aec3f10cfca9c3f22c134722164923f1686a6368`. Artifacts: `.runtime/adaptive-navigation-20260913-docking-{task,waypoint}`. The retained 1500 parking positive control remains available; no checkpoint promotion or new training occurred.

This supports further goal-conditioned learned-control work on structured docking, not general navigation or room-search autonomy. Both conditions share the privileged simulator collision shield. State dimensions, single-step action contract, velocity/acceleration limits and safety deadlines remain unchanged. Learned action sequences require compatible training and qualification; repeating one output is not implemented as a substitute. Live waypoint integration, scan-posture caching and a verified semantic room graph remain future work.

### Regression Gates

Final full backend suite: **404 passed in 948.53 seconds**, two existing dependency deprecation warnings. The earlier affected gate passed 247 tests and exposed a final-disconnect cleanup race. A deterministic cancelled-socket-scope test reproduced aborted cleanup; shielding the final ASGI cleanup fixed it. All lifecycle variants and the complete backend suite then passed without relaxing assertions or deadlines.

Frontend build passed with the existing bundle-size warning; **all 32 scripted browser tests passed in 4.5 minutes**. These include adaptive settings persistence/Start payload, Stop during preparation, both instruction-redirect modes, continuous physical movement, camera/map/3D rendering and desktop/mobile layouts. Existing Windows socket-close warnings remain. Browser inference is scripted and separate from the real-model results above. Existing live sessions and resident model processes were left untouched.

## Robot Observatory UI: 2026-09-13

The operator interface now prioritizes the world view, head-camera input, measured telemetry and a side-by-side Luna inspector. Conversation, Trace and Settings panels remain mounted when hidden so drafts and trace filters survive tab changes. Inspector tabs support arrow keys, Home and End. The trace extracts only model-supplied `reason` strings from structured calls and preserves raw payloads, refusal messages, execution results, event limits and JSON export. It does not expose hidden model reasoning or send operator-only world data to the model. Stop stays in a persistent toolbar; model configuration is labeled Configured, not a verified live connection.

Frontend build passed with the existing bundle-size warning. The full browser run passed 30/32 tests in 6.3 minutes, including real-physics camera/3D motion on desktop/mobile, continuous route tracking, map history, both scripted instruction-redirect modes, and the new observatory test. One obsolete sticky-activity assertion was corrected and passed in isolation. The settings-persistence test twice timed out while waiting for startup scans to finish; it now checks Stop during actual preparation motion, retaining its original 65-second allowance, and passed in 14.8 seconds. Dedicated route and redirect tests retain actual-navigation coverage. The full suite was not rerun after these test-only adjustments.

The new scripted observatory test covers input-frame rendering, reported reasons, malformed-JSON fallback, filters, keyboard navigation, draft retention, scene/goal placement and persistent Stop hit testing at 1440/1024/390/320 px. Screenshots are generated under `frontend/test-results/observatory-*.png`. No backend control, model instructions, weights or navigation limits changed, and no real-model inference was used for UI validation.

## Map Memory And Fixed Docking: 2026-09-13

Implemented an isolated `docking-v1` stage in `scripts/audit_architecture.py`, using six frozen starts with varied position, heading and bay location. All sources use the same paired 320x240 RGB, AgentObservation, preparation scans, clipping/deadband, one-second motion buffers and unchanged 60-second local-skill limit. A pass requires physical full-bay containment, two controller stop votes, measured wheel-derived linear speed below 0.01 m/s and angular speed below 0.02 rad/s, and no rejected command or failed execution. Evaluator coordinates are used only for setup and scoring, never passed to the controller.

| Controller | Trials Passed | Median Trial Wall Time | Median Inference Time |
| --- | ---: | ---: | ---: |
| Camera marker + odometry proportional controller | 6/6 | 19.820 s | Below timer resolution after initial detection |
| Retained SmolVLA 1500 parking checkpoint | 11/12 | 34.148 s | 0.782 s |
| Current SmolVLA 6500 primitive checkpoint | 0/12 | 13.484 s | 0.804 s |

The wall-time column includes failed trials and must not be interpreted as successful completion speed. SmolVLA uses seeds 716 and 717 across all six cases; the deterministic conventional baseline uses one run per case. Every rollout and failure is retained under `.runtime/docking-v1-20260913/{sensor,smolvla-1500,smolvla-6500}`, with source/weight hashes, case definitions, observations, camera images, proposed/bounded/requested actions, sampled execution, final rest and containment. The 1500 failure is seed 717, case 0. No retraining or checkpoint promotion occurred.

The conventional controller reuses existing green-marker segmentation and nominal flat-floor projection to acquire a target once, then tracks it with wheel odometry. It has no scene map or hidden target coordinates. This is a small structured docking benchmark with a privileged simulator collision shield, not obstacle-rich free roaming, hardware qualification, or a real-Luna navigation comparison. All three sources still use stop-ended one-second buffers here; smooth continuous route tracking remains a separate existing controller. Keep the 1500 checkpoint as a learned positive control and use this fixed benchmark for subsequent goal-conditioning and corrective-demonstration experiments.

```powershell
./.runtime/env/python.exe -m scripts.audit_architecture --stage docking --source sensor --seeds 716 --output .runtime/my-docking/sensor
./.runtime/env/python.exe -m scripts.audit_architecture --stage docking --source smolvla --checkpoint .runtime/navigation-stop-balanced-1500/checkpoint --seeds 716 717 --output .runtime/my-docking/smolvla-1500
./.runtime/env/python.exe -m scripts.audit_architecture --stage docking --source smolvla --seeds 716 717 --output .runtime/my-docking/smolvla-6500
```

Luna can now attach an optional `place_sighting` with a bounded label and visual evidence to either navigation-mode decision. The worker anchors it to that AgentObservation's measured wheel-odometry viewpoint and camera reference, rejects expired/other-episode/stopped replies, and exposes at most 32 unverified sightings to subsequent decisions. Continuous candidates include IDs of nearby sightings. Labels do not mutate occupancy, grant clearance, define room boundaries or validate success. Same-name nearby sightings replace previous evidence without accumulating confidence. They become stale after 120 seconds and expire after 1800 seconds; episode reset and successful manual relocation clear them. Sightings are ephemeral, not persistent semantic SLAM.

Footprint history contains at most 512 measured poses, sampled after 0.12 m translation or 0.3 rad rotation, with a maximum 300-second retention. Each stamp projects the current conservative robot-link bounds into wheel odometry. The default display timeout is 120 seconds, configurable to 30/120/300 seconds or Off. Stamps fade with wall-clock age even when physics is idle or requests fail; stationary samples do not refresh old trail stamps. The current footprint remains visible separately. Footprint bounds are operator overlays, not extra model inputs or proof that a previously driven route remains clear.

Verification: all **111 navigation, worker and recording tests passed** in 456.48 seconds. Coverage includes label round trips in both modes, late cancellation, Stop/takeover/reset/disconnect, placement clearing, boundedness, expiry, measured motion, smooth local tracking and docking scoring. Frontend build passed with the existing bundle-size warning. Three focused browser workflows passed: paired sensing, actual continuous-motion footprints with zero buffer stops, and scripted label/trail overlays including 1440/390/320 px captures and idle expiry during network failure. The new overlay test initially inspected the unrelated off-screen WebGL canvas; it was corrected to inspect the map canvas and passed. No real-Luna semantic-label accuracy or reduction in room-search looping is established. Full backend and full browser suites were not rerun for this update.

## Run Chat, Movement Memory And Curves: 2026-09-12

- Added active-run instruction redirects in both Luna navigation modes. The robot brakes and drains obsolete execution before applying the new goal in the same episode. Remaining decision/time budgets and accumulated tokens are preserved. Local Stop/Pause messages work at the turn limit; concurrent Stop prevents resumption. SmolVLA residency remains unchanged.
- Added bounded, shared episode memory of measured views, visits and executed-action outcomes, with separate unverified model landmark notes. Tests cover a full rotation without translation, repeat-run retention and reset/placement invalidation. The UI displays the retained movement history; models receive the sensor-derived summary. This is not a measured reduction in autonomous looping yet.
- Two real-physics curve tests verify simultaneous forward/left and forward/right motion with stable cruising commands and no contacts. These validate existing differential-drive capability, not learned SmolVLA curves. No training, model promotion, velocity-limit or scorer change occurred.
- Six focused API/curve/redirect checks passed; five memory and interruption edge cases passed; seven supervisor integration checks passed. Browser tests for continuous and SmolVLA-mode run chat passed with real physics and scripted inference, including changed instructions, retained scene/history, unchanged model-load count, Stop, reset and responsive chat screenshots.
- Initial full backend run: 389 passed, two existing arm-policy timing tests failed (one consumed 84 rather than 96 still-valid steps; one rejected a chunk that became stale during collision validation). Both passed unchanged in isolation. No deadlines or policy assertions were relaxed.
- The full backend rerun was interrupted by terminal reuse after 215 passing tests and is not counted as a completed gate. After the final correction to record post-execution SmolVLA clearance failures, eight affected supervisor, redirect, memory and curve tests passed. A fully green final backend-suite run is not established for this update.
- Final frontend build passed (`index-bcRrP3Ig.js`, `index-DcbzqzD3.css`). The full browser suite passed 29/30 tests, including both new live-chat tests; the existing continuous-navigation test exhausted its overall 45-second test limit during responsive screenshot checks after passing its movement assertions. That test passed unchanged in isolation in 31.7 seconds. No assertions or motion safety deadlines were relaxed. Editor diagnostics are clean.
- Separate preview: **http://127.0.0.1:8007/**. Kitchen to Bathroom is loaded, with no inference running, zero simulated time and an empty episode memory. Live 1440/390 px checks verified chat/textbox fit, visible memory controls, no horizontal overflow and nonblank spectator pixels. Existing simulator sessions and model processes were left untouched. Run chat becomes available after starting either Luna navigation mode. Screenshots are `frontend/test-results/chat-memory-preview-{width}.png`.

## Responsiveness And Recorded Baselines: 2026-09-12

Implemented asynchronous copied-map processing and depth-display encoding (one in-flight job), a non-actuating bounded preview renderer, a 50 ms local tracker target, bounded same-goal replanning, and optional moving Luna proposals. No model weights, SmolVLA adapter, physics timestep, velocity limits or task scorer changed. No real-Luna benchmark was run for this update.

- Before these changes, read-only inspection of the active user's run showed Luna delays of 4.5-11.3 s and a 375 ms maximum local update gap after an observed-path block. Earlier scripted routes recorded 266 ms gaps. Those are context, not a controlled A/B baseline.
- Final three camera-selected ~1.2 m physics trials arrived within 0.037-0.039 m with zero buffer stops and ~0.149 m/s measured minimum cruise speed. Runs had 107/102/131 updates in 9.422/9.391/12.344 s; maximum gaps were **140/141/156 ms**. Approximately 11 updates/s is observed, not a guaranteed 20 Hz. The right-offset path still includes a sharp final turn and takes longer.
- Local replan tests preserve the original deadline, travel counters and Stop authority. Additional checks reject detours beyond the remaining 1.8 m budget. Existing API Stop/takeover/reset/disconnect, stale-map and fully blocked-map tests pass.
- Four real-worker handoff checks cover preserved velocity and rejection after Stop, route revision or arrival. Two scripted supervisor tests prove actual translation during pending inference, one accepted continuation and cancellation-resistant late-reply rejection. These are not evidence of real-Luna completion or latency improvement.
- Background-map tests deliberately stall processing while the physics worker remains callable, then disable sensing and confirm no old map is published. Renderer cold start initially expired a selected pixel; initialization now happens before selection during a stopped scan. Existing model-camera/ownership tests remain intact.
- Full backend suite passed **382 tests in 870.78 s**, with two existing dependency warnings, before the final remaining-distance guard and recording-overflow follow-ups. Ten focused control tests and four recording tests passed after those local changes. Full browser run initially passed 27/28; a saved-sensing startup race was fixed by waiting for initial state before enabling the checkbox, and both affected workflows passed.
- Final complete browser rerun: **28 passed in 4.0 minutes**. The readiness test now waits for loaded checkbox state before deciding whether to click. A previously observed challenge-switch flake passed unchanged in isolation and in the complete rerun. Existing Windows socket-close warnings remain. Final compact-toggle CSS passed a focused desktop/mobile size test; final build and editor diagnostics pass. Bundle: `index-Chy_9-AL.js`, CSS: `index-iaa9BXp_.css`.
- Preview **http://127.0.0.1:8006/** is separate from prior servers. It is left on Kitchen to Bathroom with depth enabled, no model running, and Plan while moving unchecked by default. Desktop/mobile checks found no horizontal overflow, a fitting control, loaded paired RGB and nonblank spectator pixels. Existing sessions and loaded SmolVLA processes were not restarted. Screenshot inspection caught and corrected the new checkbox inheriting large form-input dimensions.

The earlier recorder work is also integrated: fixed ten-start kitchen-v1 cases, actual world/encoder trajectory, measured wheel/base motion, 2 Hz sampled paired media, offline replay, scorecard and failure-aware aggregate. A scripted known-map reference passed the unchanged kitchen scorer: **113.594 s first verified completion**, 118.516 s recorded duration, 83.15 simulated seconds, 5.273 m travel, zero contacts, 2,051 samples, zero drops and zero model tokens. This route is neither optimal nor learned. Replay scrubbing/playback/restart, 320px head and 160px paired RGB/depth, nonblank path canvas and no overflow passed at 1440/390px. Screenshots are `frontend/test-results/run-replay-{width}.png`.

The recorder is evaluator-only; it is not attached to existing live sessions. Privileged world/scorer fields are segregated from model inputs. Bounded-buffer overflow flags incompleteness, retains the media referenced by surviving samples and suppresses completion-time/autonomy scoring. Sampled distance/contact/stop metrics are diagnostic, not certified motion capture; the stop classifier does not automatically decide which stops were avoidable. No fine-tuning export was performed.

## Luna Continuous Kitchen Navigation: 2026-09-12

RGB + depth can now be enabled during controller ownership and stays on across scene resets in the same server, with a fresh episode map. Disabling sensing and executing manual scans remain ownership-protected. The browser remembers the fold-arms preference, defaults it on, and passes it to continuous Start. Continuous local control is a distinct default selector beside legacy SmolVLA primitives. Residency is unchanged; this mode does not invoke SmolVLA.

### Real-Model Evidence

Reports remain under `.runtime/kitchen-continuous-v1` through `v18`, plus `v16-repeat`: **19 development runs of changing implementations**, not a fixed-version benchmark. Total: 8,634.7 wall seconds, 3,279,674 input tokens and 232,873 output tokens. **One physics pass, nine false completion claims.** No manual placements, fixed teacher route, cloud provisioning, training or checkpoint promotion occurred. Deployment `gpt-5.6-luna` was configured; underlying model identity was not independently verified.

- **Verified pass: v16.** Kitchen to Bathroom completed in 235.907 control seconds, 138.45 simulated seconds, 10 Luna decisions, five routes/five arrivals, **zero buffer-empty stops**, maximum update gap 0.297 s. Full-base/wheel containment, ground contact and rest passed the unchanged scorer. Fixture descriptions are model-generated and not semantically graded.
- **Unchanged v16 repeat: failed.** 600-second deadline, six routes/three arrivals, zero buffer stops. It exposed inspection incorrectly gated by destination-surface matching; the subsequent fix permits sensing before final checks.
- **Later v17/v18: failed.** v17 reached containment but expired during arrival inspection; v18 selected an incorrect room/surface and claimed completion. Final code adds settled transitions and bounded preview rendering, but reliable completion is **not established**. The earlier pass must not be presented as a repeatable benchmark or learned SmolVLA result.
- Earlier trials exposed missing near-floor coverage, stale panoramas, corridor/room pixel ambiguity, sharp-corner tracking and frozen residual velocity after braking. Diagnostics and focused regressions address these mechanisms. Mapping remains odometry-based, conservative and static; color/fixture interpretation remains fallible.

Smoothness applies within accepted local routes. Scans, sharp corners, safety guards, arrivals and waiting for Luna still produce deliberate pauses. There is no pipelined planning or uninterrupted whole-challenge motion.

### Regression Evidence

- Full native-Windows backend suite: **371 passed**, two existing dependency warnings, 740.06 s. Initial failures fixed: enabled-sensing leakage across app lifetimes, unpublished disconnect buffer state and unnecessary braking at shallow corners. Thirteen affected checks passed before the full rerun.
- Frontend build passes with the existing large-bundle warning. Initial full browser run passed 27/28; startup exposed excessive 640x480 scan rendering cost. Continuous sessions now use 320x240 throughout and restore dimensions on exit. The affected browser check passed in 57.5 s afterward. Browser inference is scripted, separate from the real-model evidence above.
- Final complete browser rerun: **28 passed in 4.1 minutes**. Twelve focused backend checks passed after the rendering/cleanup adjustment. The final residency-label correction passed its two affected browser checks; launcher PowerShell syntax and edited-file diagnostics passed. Final bundle is `index-agwKg0rd.js`.
- Updated preview: **http://127.0.0.1:8005/**, with prior servers/sessions untouched. Kitchen to Bathroom is loaded, RGB + depth enabled, fold preference on, continuous control selected, and no model started. Live desktop/mobile checks at 1440/390 px found no horizontal overflow, a fitting controller selector, 47 sampled spectator colors and loaded 160px paired RGB. Screenshots: `frontend/test-results/kitchen-continuous-{width}.png`. Physics remained at zero simulated seconds and SmolVLA stayed unloaded. The shared browser's hidden-page stability wait was avoided with DOM scrolling; no application timing guard was disabled.
- Safety coverage includes observation-only inputs, cancelled late decisions, sensing enable/protected disable, episode isolation, panorama expiry, surface consistency, connected floor scans, settling, Stop/takeover/reset/disconnect, stale maps, blocked routes, leases/deadlines and no-progress exits.

## Reset And Model Residency Clarification: 2026-09-12

- Investigated a report of model reloading after scene reset. Live servers on ports 8002, 8003 and 8004 each reported a ready resident model and load count 1, with separate process IDs 4836, 40836 and 22308. These are independent backend instances, not three loads in one server. No reset-triggered weight unload was reproduced, so the backend ownership/lifetime code was not changed.
- The UI now identifies reuse during startup/warm-up and waiting for a previous in-flight prediction, using the resident model's actual state. Server address, load count and process ID remain visible outside the per-run progress panel, including after scene reset. Genuine cold loading remains labeled as loading.
- Six existing resident lifecycle checks passed, covering reuse, cancellation-resistant replies, loading through a cancelled run, failure-only reload and API lifecycle. The API test was strengthened to call Reset explicitly after each run and assert a new physics episode with the exact same resident process/load count; both parameterized cases passed. Two focused browser tests passed for reuse/loading labels and real-physics supervised sessions across challenges; inference in those tests is scripted.
- Frontend build and edited-file diagnostics passed. No real-model inference, manual scene reset, weight change, backend restart or process shutdown was performed for this follow-up. Full backend/browser suites were not rerun for the display and test-only changes.
- Read-only observation of a subsequent user-started run on port 8004 confirmed the scene changed from episode `78ecab0c-7307-4460-883c-4b6fa56f80b1` to `f4580cac-4faf-4503-ba02-9df98916e986`, while process **22308** and load count **1** remained unchanged and inference was active. The attempted UI refresh was skipped because the user was testing; the new labels load on a later normal refresh.

## Continuous Local Navigation: 2026-09-12

Added an opt-in camera-selected continuous local controller, separate from Luna/SmolVLA inference. The new scan/start endpoints require a connected operator and exclusive ownership; targets come only from retained paired RGB/depth and measured odometry. A SciPy graph planner uses observed floor plus whole-robot clearance, and a worker-owned tracker replaces short future motion without draining every buffer. No hidden map, teacher route, task scorer, model action or manual teleport selects the route.

Focused results:
- A direct physics tracker test covered 1.2 m with small heading correction, zero buffer-starvation stops and deterministic braking within 0.04 m.
- Three full sensor/worker trials scanned the unchanged parking scene with explicitly folded arms, then selected nearby floor pixels with left/center/right offsets. All three arrived within 0.035-0.039 m, with **zero buffer stops**. After reducing only the normal preview resolution during continuous motion, runs took 9.3-10.4 seconds after scanning and produced 67-73 local updates. Measured wheel-derived minimum cruise speed was 0.1481-0.1499 m/s, excluding startup and final approach. Worst update gaps were about 0.266 seconds; this is approximately seven updates/second, not a claimed 10 Hz guarantee.
- Unknown/disconnected destinations and footprint-adjacent obstacles are rejected by the path tests. Floor connectivity initially failed safely around the extended arms; the validated scan option folds arms through collision-checked commands. The map uses adjacent depth-supported floor quads instead of blanket filling occluded space. Its anchored start-disk bootstrap and flat-floor/odometry assumptions are detailed in MECHANICS; this is not a general navigation or mapping benchmark.
- Five tracker fault tests verify stale sensing, newly blocked lookahead, expired buffer authorization, Stop and the local-goal deadline clear pending motion without advancement. Six API/worker tests cover Stop, takeover, reset, disconnect, stale map and blocked map, as well as rejection of competing starts and sensing changes during movement. No late command restarts the robot.
- The browser workflow passed using real depth and physics with no model calls: floor scan, pixel selection, visible route/progress, live head-camera changes during movement, disabled manual/LLM starts, arrival with zero buffer stops, and responsive checks at 1440/390/320 px. Screenshots are `frontend/test-results/continuous-{width}.png`; the existing sticky activity strip can appear over a long mobile element capture. No page layout overflow was found.
- Final backend gate: **363 tests passed in 562.14 seconds**, with the two existing dependency warnings. Frontend build and edited-file diagnostics passed. The first full browser run had one challenge-selection timing failure; that test passed unchanged in isolation, and the complete rerun passed **26 tests in 2.7 minutes**. No assertions were relaxed or tests skipped.
- A new preview runs at **http://127.0.0.1:8004/**; existing ports 8000/8002/8003 were left untouched. A live operator-style scan-and-click check in its parking scene selected a depth-backed point 1.71 m ahead. It arrived with 0.036 m remaining in 13.969 seconds, 100 updates, zero buffer stops, and a maximum measured update gap of 0.25 seconds. WebSocket observation recorded 85 distinct camera frames during motion, approximately 0.15 m/s cruising commands, zero reported contacts, and zero model tokens. This point was not the parking objective center; the challenge remained in progress and no challenge success is claimed. The stopped destination result and scanned map remain available in the preview.

Scope: one short, operator-selected observed destination at a time. No continuous Luna-goal handoff, full-task success, robust obstacle detouring in arbitrary scenes, or new fine-tuning is claimed. New obstacles trigger a safe stop rather than an automatic reroute. Existing learned control remains available and unchanged.

## Robot Camera Visibility: 2026-09-12

- The shared port-8002 page had an empty head-camera panel despite its current PNG returning HTTP 200. A direct browser probe showed the image load event completed (320x240), while `Image.decode()` remained pending in the hidden integrated browser tab. Head-camera and spatial-pair loaders now rely on successful load events instead of that promise. Head-camera fetch/image loading is bounded to four seconds and retries after failure; reconnect reinitializes the loader while retaining the last displayed frame. Disconnected/reconnecting views are labeled as last-frame data.
- Added an expandable first-person camera dialog using the same object URL/frame as the inline view, with uncropped aspect-preserving rendering, Stop, close, Escape and focus return. It is independent of the spectator camera and does not change model inputs, physics, or inference. Exact model-submitted image history remains in the exchange feed.
- Build and edited-file diagnostics passed. The full browser suite passed **25 tests in 2.5 minutes** before the final load-event correction; the four affected camera/spatial workflows then passed against the final build in **18.6 seconds**. Tests cover failed fetch recovery, a never-settling decode promise, actual head-motion frame updates, equal inline/expanded frame URLs, Stop, and responsive dialog geometry at 1440/390/320 px. Screenshots `frontend/test-results/robot-camera-{width}.png` were inspected. The existing Windows socket-close warning remains nonblocking. No backend code changed for this follow-up.
- After checking the shared page was idle, refreshed it and opened the enlarged camera. The rendered `live-94.png` measured 320x240 with 59 distinct sampled colors; episode ID stayed the same and simulation time remained 11.35 s. No reset, camera motion, model invocation, or backend restart was performed. The active controller on port 8003 was left untouched. The new frontend is served by the existing app servers after page refresh.

## Spatial Sensing: 2026-09-12

Implemented step 1 of the navigation-first plan: opt-in paired RGB/metric-depth capture, calibrated projection, a sensor-only rolling map, a browser inspection panel, and a tested optional compact travel posture. Navigation, planning, learned policies and live defaults are not changed. The source sensor/map contract is separate from AgentObservation, so no depth/scene metadata is silently added to existing model requests.

Focused verification:
- Real camera tests check axial depth against a known wall, projection under three head directions, invariance to the encoder frame's origin, no physics advancement during capture, and no scene identities in sensor data. Invalid, self-occluded and no-return depth remain null.
- Mapper tests verify observed floor/obstacles, unknown space behind a screen and behind the robot, rejected stale/out-of-order/cross-episode frames without map mutation, and bounded rolling-window behavior.
- Worker/API tests verify matching RGB/depth timestamps and frame IDs, opt-in behavior, model-observation sequence isolation, reset/disable/relocation cache invalidation, spatial updates during real navigation, and Stop with continued stationary sensing. A first drive fixture correctly hit the nearby bench cube's clearance stop; the timing check uses the open parking fixture instead, with safety rules unchanged.
- Compact posture tests use actual collision-checked arm motion in parking, kitchen and recharge, reduce the forward envelope by more than 12 cm, reduce bounding radius, and finish without contact. No teleport or URDF changes, and no automatic posture switch.
- Focused Playwright workflow passed with real physics: enable/disable, paired-image decoding, head movement updates, nonblank map canvas, no horizontal overflow at 1440/390/320 px. Captures are under `frontend/test-results/spatial-{width}.png`. Existing sticky status UI can overlay part of a long mobile element screenshot while scrolling; the sensor views remain reachable. Paired images show robot silhouettes masked only in depth and observed floor/obstacle cells with unknown surrounding space.
- Ten explicit bench samples measured capture + self-filter + map + depth-PNG work at approximately 37 ms median / 41 ms p95 (an earlier sample measured 42/46 ms). This is a small unloaded-machine sample, not a hard-real-time guarantee or a 10-20 Hz navigation claim. Spatial capture still uses the authoritative worker at a 2 Hz target; asynchronous sensing/SLAM/path control remain later milestones.
- Full backend regression: **346 passed in 433.10 seconds**, with two existing dependency deprecation warnings. The complete browser suite initially passed its assertions but exposed a reset-time sensor polling error; closed-episode metadata/frame reads now return 409/404, with **two focused spatial API tests passing** afterward. Final browser rerun: **22 passed in 2.1 minutes**, with no closed-episode server errors (the existing Windows socket-close teardown warning remains). Frontend TypeScript/build passed with the existing large-bundle warning.
- Updated preview runs at **http://127.0.0.1:8003/**, leaving older ports 8000/8002 untouched. Live opt-in inspection confirmed matching 160x120 RGB/depth frame IDs, fresh map data (852 observed-floor, 81 obstacle and 24,667 unknown cells in the initial bench view), nonblank map pixels and a 0.507 m conservative footprint radius. Enabling sensing left physics at zero simulated seconds and started no model inference. A later user-started controller run was detected; the planned manual camera action was skipped rather than interrupting it. No real-model success is claimed.

Limitations: nominal upright flat-floor projection, encoder drift uncorrected, sparse floor endpoints, static obstacle retention, and no collision-inflated traversability map. The map advertises `planning_ready=false`. Current policy inputs, task scorers, motion bounds and cancellation rules remain unchanged. No real-model task validation or fine-tuning was performed for this sensing slice.

## Navigation Budget Expansion: 2026-09-12

Implemented a staged increase of isolated navigation budgets from the previous 40-turn experiments to **80 turns**, **120 turns**, and finally **30 minutes with no overall request-count cap**. The evaluator now uses the time-only budget by default. This changes evaluation budget handling, not the model, controller instructions, safety thresholds, scene scorers or live browser settings.

### Implementation And Safety

- `NavigationEvaluationBudget` is an internal controller-constructor option, not a field accepted by the public Start API. It permits 1-1,000 turns or a null turn limit, and a positive wall-time budget up to 3,600 seconds. The time-only iterator is genuinely count-unbounded, not a hidden large finite request allowance. Supported evaluation paths are `luna_navigation`, `single_step` and `navigation_plan`; chat, arm-policy and local-only runs reject the override.
- `scripts/evaluate_supervised.py` defaults to 1,800 seconds with no count limit. Explicit `--turns` retains request-limited comparisons, with an optional `--session-limit-s` ceiling. `--time-limit-s` cannot be combined with `--turns` or a second session ceiling. Models preload while stationary before the measured run; scene initialization/preload are excluded, while controller warm-up, motion, inference and feedback waits count. Every result retains `budget`, `preload_s`, `evaluation_elapsed_s` and `termination_reason` separately from total wall time.
- Time expiry cancels pending inference and brakes through the existing cleanup path. Returned late actions face a deadline check before actuation. The timeout context's actual expiry distinguishes overall deadline exhaustion (`limited`) from an individual inference timeout (`error`), avoiding Windows clock-granularity misclassification. Stop/takeover/reset/disconnect retain precedence over late replies. The new report fields also distinguish an agent completion claim from a physics pass.
- Browser/API request defaults remain unchanged: supervised navigation still caps at 80 turns and 1,200 seconds when no internal evaluation override is present. All velocity, clearance, observation freshness, per-buffer lease, 60-second skill deadline, inference timeout, and eight-local-step-per-guide limits remain. Time-only tests still end early on model termination, safety faults, or operator cancellation; they are not instructions to ignore Stop or run for the full interval.

### Real-Model Results

All six tests used real configured Luna Responses inference and resident recovery-8500 SmolVLA, high reasoning, unchanged task goals and fresh isolated workers. Source hashes in all three experiment manifests matched. Each stage ran kitchen then recharge; these are separate runs, not continuations or fixed-seed cloud replays. No manual placements, browser reset, resource provisioning, checkpoint promotion or weight updates occurred.

| Stage | Task | Actual Luna Turns | Completed Local Commands | Control Time | Ending | Physics Pass |
| --- | --- | ---: | ---: | ---: | --- | --- |
| 80 turns / 1,200 s | Kitchen to Bathroom | 80 | 216 | 953.2 s | Request limit | No |
| 80 turns / 1,200 s | Recharge | 80 | 271 | 1,137.0 s | Request limit | No |
| 120 turns / 1,800 s | Kitchen to Bathroom | 30 | 92 | 491.6 s | False agent completion claim | No |
| 120 turns / 1,800 s | Recharge | 120 | 189 | 1,071.1 s | Request limit | No |
| Time only / 1,800 s | Kitchen to Bathroom | 135 | 331 | 1,488.8 s | False agent completion claim | No |
| Time only / 1,800 s | Recharge | 138 | 257 | 1,311.8 s | `BATTERY_EMPTY` | No |

Raw reports/traces/input images: [80-turn stage](../.runtime/navigation-budget-80/summary.json), [120-turn stage](../.runtime/navigation-budget-120/summary.json), [time-only stage](../.runtime/navigation-budget-time-1800/summary.json). Preload was about 74 seconds for the first task in each batch and approximately zero for the second. These are excluded from the control-time column. The original saved reports predate the added `unverified_completion_claim` field; their separate physics/outcome fields already establish the false claims, and reports were not rewritten.

Both time-only runs exceeded 120 turns, establishing that the former count cap was removed. Neither reached its wall-time deadline: one ended on Luna's own claim, the other on the unchanged battery safety condition. Actual deadline braking/classification is verified by scripted regression tests, not claimed as observed in these two real trials. Aggregate returned usage was 2,871,857 input and 256,976 output tokens across six runs; unreturned/cancelled requests may add billing not represented in these counts. Deployment identity was not independently verified, and cloud sampling was not controlled.

### Training Decision

Longer budgets produced more commands but no full-task pass. Traces show repeated target selection/detours, incorrect base-inside-room judgments, and battery exhaustion on the recharge attempt. This does not isolate a new correctable action-label defect or supply expert recovery targets. No additional fine-tuning was performed: training on these failures without corrected actions would reinforce the same errors, and the prior 4,000-update task-skill experiment had not established a task gain. Preserve these traces for curated docking, route-memory and energy-aware recovery work rather than increasing identical training or runtime indefinitely. The two-distinct-challenge goal remains unmet.

### Verification

- Before real trials: **174 controller/API/navigation tests passed**, with two existing dependency deprecation warnings. Edited-file diagnostics and Pylance syntax checks passed.
- Subsequent focused checks: **22 tests passed** covering explicit/default budgets, timeout versus inference failure, deadline cancellation during supervisor/local inference and motion, plus Stop/takeover/reset/disconnect with both request-limited and time-only controllers. Cancellation-resistant replies authorize no late motion. Public Start payloads reject the internal budget field.
- Final regression gates: **336 backend tests passed in 422.73 seconds**, with the same two dependency deprecation warnings; frontend build passed with the existing large-bundle warning; **21 Playwright tests passed in 2.1 minutes** on isolated port 8001 using real physics and scripted inference. These verify integration and safety behavior, not real-model task competence. The live port-8002 app and its resident checkpoint were not restarted or changed.
- The model-facing observation boundary remains unchanged. The evaluation override and budget are recorded in operator-side traces/reports, not inserted into AgentObservation.

## Task-Skill Training And Tests: 2026-09-12

**Completed real fine-tuning and testing; no new full-task success established.** Kept the live recovery-6500 resident model and UI unchanged. Added an experimental bounded robot-relative waypoint instruction helper, isolated task-data recorder/benchmark, and reused the verified LeRobot exporter with a loader for the new curriculum. No motion guards, scorer thresholds, scene geometry, cloud resources or dependencies changed. Physics and CUDA remained in separate native Windows environments.

### Training Evidence

- `.runtime/task-waypoint-demonstrations-v1`: 16 episodes, 997 paired frames, all saved-action replays verified at maximum state error 0.0. Tasks cover docking, doorway traversal into the bathroom, southern obstacle detour to the orange survey zone, and reverse/re-approach. Recharge records stop after its first objective; no complete recharge demonstration is claimed. Earlier northern-route clearance failure and depleted-battery round trip remain in rejected pilot directories and were not exported.
- `.runtime/datasets/milo-task-waypoints-v1/navigation-export.json`: 1,701 exact-readback frames, comprising 997 task frames plus 704 split-matched primitive-retention frames from recovery-demonstrations-v2. Training: episodes 0-11, 1,278 frames. Validation: episodes 12-15, 423 frames, sampled every third frame for 143 loss samples. Train-only action normalization, unchanged 20-state/two-action architecture, image/state identity normalization. Offline teacher uses fixture routes, but model input is restricted to the paired image/measured state and explicit relative-goal instruction. Relative goals in this controlled benchmark are teacher assistance, not inferred navigation.
- [10,500-step report](../.runtime/navigation-task-waypoints-10500/report.json): 2,000 new updates at 1e-5, batch four, held-out loss 3.638502 -> 0.600645, 716.86 s. Weights SHA-256 `34ad62f439a93254d3053ddb8e7199e98712780f1f1c474a7861cb9d86faf228`.
- [12,500-step report](../.runtime/navigation-task-waypoints-12500/report.json): 2,000 further updates at 3e-5, loss 0.600637 -> 0.235987, 712.98 s. Weights SHA-256 `bc30a9f4bd700e8fb71f0eb8aec3f10cfca9c3f22c134722164923f1686a6368`. Both strictly reloaded, 99,880,992/450,046,176 trainable parameters, frozen vision/language backbone, 3,320.735 MiB peak allocated GPU memory, fresh AdamW state per continuation.

### Real-Model Tests

The new skill benchmark fixes four evaluation-only nearby offsets (cases 16-19, never recorded/exported). It supplies the same bounded teacher-selected waypoint sequence to each policy, resets the local inference RNG between cases, and retains all results/images/raw actions. Measured odometry decides waypoint error only for scoring/teacher advancement; the local model sees the relative instruction. All actuation uses real physics and validated NavigationRuntime with unchanged bounds, deadlines and stop conditions.

| Checkpoint | Assisted routes completed | Local waypoints completed | Main outcome |
| --- | ---: | ---: | --- |
| recovery-8500 | 0/4 | 0 | Repeated rotation; deadline stops |
| task-waypoints-10500 | 0/4 | 0 | Translation learned, but wrong-direction/overshoot and clearance stops |
| task-waypoints-12500 | 0/4 | 2 | One detour and one retreat waypoint, followed by timeout/premature stop |

Reports: [baseline](../.runtime/task-waypoint-baseline-8500/summary.json), [10,500 candidate](../.runtime/task-waypoint-candidate-10500/summary.json), [12,500 candidate](../.runtime/task-waypoint-candidate-12500/summary.json). One 10,500 parking trial ended physically parked after timeout braking but failed the requested waypoint, so it is not a successful route or autonomous result. Candidate evaluations informed the second continuation; these are development evaluations, not an untouched benchmark. Layout diversity remains limited.

Paired non-actuating primitive probes (same three scene images, five commands, three predictions each): both models produced correct signs for all 36 movement predictions. Existing deadband accepted 6/9 baseline versus 7/9 candidate hold predictions. Direction signs do not certify cross-axis drift or safe control. [Baseline probe](../.runtime/task-waypoint-retention-8500/summary.json), [candidate probe](../.runtime/task-waypoint-retention-12500/summary.json).

Four additional full-task real Luna+SmolVLA trials used the existing primitive-based supervisor: two tasks per checkpoint, high reasoning, 40 turns, unchanged prompts/limits and no manual placements. The new waypoint instruction was **not** inserted into live supervision, so these are compatibility/end-to-end retention tests, not autonomous tests of waypoint conditioning.

| Checkpoint / Task | Physics Success | Luna Turns / Local Commands | Wall Seconds |
| --- | --- | --- | ---: |
| recovery-8500 / Kitchen to Bathroom | No; false agent completion claim | 40 / 117 | 526.1 |
| recovery-8500 / Recharge | No; turn budget | 40 / 138 | 443.0 |
| task-12500 / Kitchen to Bathroom | No; turn budget | 40 / 123 | 534.4 |
| task-12500 / Recharge | No; turn budget | 40 / 143 | 478.0 |

[Baseline task reports](../.runtime/task-supervised-baseline-8500/summary.json), [candidate task reports](../.runtime/task-supervised-candidate-12500/summary.json). Cloud deployment `gpt-5.6-luna` was configured, underlying identity not independently verified; sampling seed is uncontrolled. No second distinct autonomous challenge completion or candidate superiority is established.

### Decision And Checks

Do not promote either candidate or continue identical training blindly. Text-encoded metric goals remain unreliable near sign/turn/stop boundaries; the observed loss reduction is not a task-performance gain. Further work should test numeric goal conditioning and corrective-state coverage while preserving Luna's ownership of semantic route decisions. More training on teacher-selected routes alone cannot establish route discovery or battery-return competence.

The complete navigation regression file passed **49 tests in 111.15 seconds**, including finite/bounded frame-invariant waypoint text, real physical demonstration/replay, rejection of private training fields, benchmark scoring/cleanup, and existing safety/cancellation checks. The benchmark's scripted competent controller completed a parking route; its scripted early-stopping counterpart was correctly rejected. These scripted checks are separate from the 16 real-model closed-loop trials and 90 non-actuating predictions above. Edited-file diagnostics, task-script Pylance syntax, and whitespace checks passed. The full backend and browser suites were not rerun; no frontend or active production control path changed.

## Control-Mode Comparison: 2026-09-12

**Decision: do not switch the browser default to the existing `navigation_plan` mode.** Twelve real-model trials found no task-success improvement. This also does not establish single-step as a reliable replacement for the current Luna-supervised SmolVLA flow. A persistent high-level plan within that supervision is a separate, untested proposal; it was not implemented here.

### Protocol

- Compared existing direct-Luna `single_step` and `navigation_plan`, two runs each on `park`, `kitchen_bathroom`, and `recharge`. SmolVLA was neither loaded nor invoked in this comparison. These are real Responses calls, not scripted inference or additional fine-tuning.
- Both modes used configured deployment `gpt-5.6-luna`, high reasoning, 60 turns, 600-second session budget, 0.25-second feedback interval, 320x240 paired head images, two-image limit, and 8,192 retained-context tokens. Model inputs remain camera/AgentObservation/goal and allowed history, never evaluator or spectator state.
- Order was single-step round 1, plan round 1, plan round 2, single-step round 2. Round 2 reversed challenge order. Fresh isolated physics workers preserved the original scenes and scorers; all 12 runs had zero manual placements. Starting camera hashes matched across all four runs of each challenge. All experiment manifests recorded the same source hashes. The live browser was not restarted, reset, or switched.
- This compares the modes **as implemented**, not planning alone: single-step permits 0.3 m/s and 0.8 rad/s, while plan mode permits 0.15 m/s and 0.5 rad/s, a two-second buffer, ordered skill gates, and a 60-second wall-clock deadline per skill. No guard or limit was relaxed. Both existing direct controllers can terminate on private physics completion without feeding that score to the model; this is not proof of model self-verification.
- The cloud sampling seed was not controlled, and the underlying deployment identity was not independently verified. Two repetitions on three known fixed scenes are a small development comparison, not a general success-rate estimate or statistically significant ranking.

### Results

| Challenge | Single-step physics passes | Navigation-plan physics passes | Observed failure pattern |
| --- | --- | --- | --- |
| Park in the Bay | 1/2 | 0/2 | Single-step first run falsely claimed completion at 0.481 m; second passed at 1.078 m. Plans timed out during `cross`, at 0.870 m and 0.557 m. |
| Kitchen to Bathroom | 0/2 | 0/2 | Single-step spent 56/60 and 50/60 calls moving the head. Both plans timed out during `locate_doorway`. |
| Remember and Recharge | 0/2 | 0/2 | Neither mode completed the survey. Single-step exhausted its turns; both plans timed out during `locate_doorway`. |
| Total | **1/6** | **0/6** | All six plans hit `SKILL_TIMEOUT`. |

| Measure, six runs per mode | Single-step | Navigation plan |
| --- | ---: | ---: |
| Total elapsed seconds | 1,224.953 | 499.186 |
| Supervisor turns attempted | 254 | 98 |
| Received responses | 254 | 93 |
| Reported input tokens | 1,034,029 | 432,481 |
| Reported output tokens | 42,758 | 16,963 |
| Received-response latency median / p95 | 3.407 / 6.688 s | 3.687 / 8.297 s |

Every received response exceeded two seconds, so this deployment did not demonstrate reliable refill within the motion-buffer horizon. Plan mode's lower time/token totals reflect earlier failure, not better successful-task efficiency. Token totals are returned SDK usage, not a billing reconciliation; cancelled/unreturned requests may incur additional usage.

No contact was present in plan-mode recorded feedback. Single-step recharge round 2 contained seven feedback observations with contact and 13 action-result records with contact during motion; these can overlap and are not distinct collision counts. No collision-free guarantee is inferred from sampling or zero tool errors. Plan recharge round 2 also returned two `INVALID_ARGUMENT` errors. All failures remain retained.

The results favor improving task-appropriate skill sequencing, scan-to-motion transitions, measured progress, and local execution under Luna supervision before another switch decision. The fixed `inspect_room -> locate_doorway -> approach -> cross` protocol was especially unhelpful for a recharge detour. Simply increasing deadlines or weakening clearance checks is not supported by these trials.

### Artifacts And Reproduction

- Raw summaries: [single-step round 1](../.runtime/mode-comparison-single-r1/summary.json), [plan round 1](../.runtime/mode-comparison-plan-r1/summary.json), [plan round 2](../.runtime/mode-comparison-plan-r2/summary.json), [single-step round 2](../.runtime/mode-comparison-single-r2/summary.json). Each batch also contains an `experiment.json` manifest plus per-challenge reports, full trace history, exact input images, and a terminal image. Local `.runtime` artifacts are not committed.
- Run `./.runtime/env/python.exe -m scripts.evaluate_supervised --mode single_step --challenges park kitchen_bathroom recharge --output .runtime/mode-comparison-single-new --turns 60 --reasoning high --images 2 --context-tokens 8192`; substitute `navigation_plan` and a fresh output directory for its pair. Existing outputs are never overwritten.
- The evaluator now supports both direct modes without a local checkpoint, reports physics success separately from model claims, records source hashes and latency/tool-error metrics, and retains the existing supervised-navigation evaluation option. Three focused scripted harness checks passed before real trials; the final complete navigation test file passed **45 tests in 62.56 seconds**, including existing safety/cancellation coverage. These are scripted regression checks, not learned task evidence. Edited-file diagnostics and evaluator Pylance syntax checks passed. No production controller or frontend code was changed for this comparison; the full backend and browser suites were not rerun for this script/documentation-only change.

## Resident SmolVLA: 2026-09-11

- The browser backend now owns a resident local inference process. Normal completion, Stop, takeover, reset, challenge changes, voice-mode changes and operator disconnect close only the run session; model weights remain in GPU memory. Startup continues safely if the initiating run is stopped. Queued work from closed sessions is skipped, and an in-flight cancelled reply is drained and discarded before another session can use the stream. Failed workers are closed and reloaded on next Start; app shutdown or explicit idle-only **Unload local model** releases the process.
- A strict reset message resets policy state and the random seed for each new run without reloading weights. Inference stays serialized, goal/image/observation ticket boundaries remain intact, and no motion/freshness/clearance limits were relaxed. Residency status is separate from robot-run state and persists across episode reset. The CLI evaluation remains a separate short-lived process.
- **Automated:** 119 controller/API tests passed, including resident reuse, stopped loading, stale replies across replacement episodes, reset/Stop/disconnect retention, explicit unloading and shutdown. The browser suite had 37 passes and one voice save/read race; the test now awaits the save response and passed its isolated rerun. Focused resident/current-scene/loading browser tests passed. Build, launcher dry run and edited-file diagnostics passed.
- **Real GPU reuse:** two one-command Apartment Search runs completed without runtime errors. Cold session `9135c0aa-b9a9-41dc-ab99-7dfab668d469` took **61 s**; after an episode reset, warm session `3b58db03-603f-46aa-807f-40dc18f8cfec` took **7 s**. Both include session warm-up, scripted scans and one model command; these are total run times, not inference-only latency. Both used process **40696** with model load count **1**, and each advanced 4.6 simulated seconds. A further reset preserved the same ready process and restored the normal 30-request setting. The shared browser's wait timed out during the second check, but the matching server session and rendered UI confirmed successful completion in seven seconds. No task-success or general-navigation claim is made from these short tests.
- The two-near-zero-prediction stopping rule is unchanged. Persistent weights improve iteration time but do not fix premature stops or route finding. Recovery fine-tuning and event-driven Luna supervision were discussed, not implemented in this update. Old notes below that say every run unloads its model describe the earlier implementation.

## Browser Verification Follow-Up: 2026-09-11

- Final complete Playwright run: **37 passed in 4.4 minutes**, with real physics and scripted inference on isolated port 8001. This supersedes the outstanding browser regression failures recorded in the previous section. The known Windows socket-close teardown warning still appeared without failing assertions. Frontend build and edited-file diagnostics passed.
- Corrected test-only issues: the scripted arm fixture now supplies eight bounded samples instead of 50 and holds a pending reply after initial movement, so the cancellation test does not keep streaming while taking screenshots. Production motion bounds, freshness limits and controllers are unchanged. Reset assertions now expect the user's remembered arm/navigation mode with cleared episode state. The manual recharge test waits for the actual successful `wait` response before inspecting its action timeline.
- Related backend verification: **32 passed** across local navigation lifecycle and supervised-policy failure tests. This checks the shared scripted fixture as well as current-scene preservation, full goal transport, pending-reply cancellation and cleanup. This follow-up did not rerun the entire physics suite.
- Live port-8002 observation used **Clinic Supply Delivery**, with the real local checkpoint. The first observed session `40415ea5-e91b-472d-857a-0c4408e14f4f` loaded in 64 s, started driving at 69 s and stopped after eight requests at 83 s without completing the challenge. The shared app was reset/restarted during observation, so aggregate pose/frame counts are not a controlled single-run benchmark. A subsequent observed run also stopped after eight requests with completion unverified. Approximately **0.234 m** displacement was observed; the selected goal remained the clinic goal, with zero cloud tokens in those local runs.
- Live rendering checks: desktop WebGL samples changed (569 to 597 distinct sampled colors); mobile 390px/320px views were nonblank (458/454 colors), with no horizontal overflow or page JavaScript errors in the probe context. Captures: `frontend/test-results/real-clinic-desktop.png`, `real-clinic-390.png`, `real-clinic-320.png`. The observed local run ended stopped with zero buffered motion, and no local inference process remained at the cleanup check. The model stopped itself before the probe's planned Stop click; interactive Stop/cancellation is verified by the automated tests, not claimed as a manual Stop on this completed live run.
- A different controller run began in the shared app after the local checks. It was not interrupted; only the extra test browser context and observer socket were closed. The live app remains available on port 8002. **Browser integration is working; reliable clinic-route completion remains an unverified model capability.**

## Current-Challenge SmolVLA Control: 2026-09-11

- **Start LLM control** now starts local SmolVLA in the currently loaded challenge, without replacing the scene, episode or robot position. Local navigation is the initial browser mode; explicit alternatives are remembered. Removed the parking-only, fresh-episode and fixed-goal restrictions. The live progress indicator names the selected challenge instead of hardcoding case 15. Cloud and arm modes remain separate.
- The current Robot goal is sent with only AgentObservation and its paired head image. A navigation-specific request accepts up to 2,000 characters, independently of the arm request. Short internal skill labels avoid violating the existing navigation schema for long challenge descriptions. The saved preprocessor originally truncated at 48 tokens; navigation inference disables truncation while keeping short-goal padding unchanged. The actual saved tokenizer pipeline retained all **124 recharge-goal tokens**, and the short parking token tensor remained identical. No weights or normalization files changed.
- **Real recharge-scene run:** episode `b165e335-75cb-4226-89dc-202e2197b87e`, model session `12519618-d098-4d6d-a04b-a8df3909a90d`. The scene and full goal were unchanged across Start. Loading completed at 126 s, driving started at 133 s, and the controller stopped at 148 s after **five executed model requests**, **0.246 m base displacement**, **62 pose samples**, **54 camera frame references**, and **8.6 simulated seconds**. All five logged velocity outputs were unsaturated. The 3D canvas had nonblank pixels and the trace instruction matched the selected goal. **Recharge was not completed:** a subsequent request was rejected with `CLEARANCE_STOP: Buffered motion lacks whole-robot clearance.` The robot remained stopped with zero buffered motion, zero cloud tokens and an unverified task outcome. This proves connected current-scene control, not successful obstacle routing or recharge planning.
- The first real attempt timed out during model startup at 120 s without motion. Cold-start allowance is now 300 s with loading-stage logs; Stop remains available during loading and the two-second observation freshness limit and motion leases are unchanged. The subsequent 126 s load confirms why the previous startup budget was insufficient on this run.
- **Focused verification:** 26 local-navigation backend tests passed, plus the three-scene real-physics/scripted-inference test for parking, warehouse and recharge. The selected-warehouse browser workflow passed with preserved episode/goal, measured displacement, changed camera URL and WebGL pixels, desktop/390px/320px layout checks, takeover and challenge switching. Build, launcher parsing/dry run and edited-file diagnostics passed.
- **Regression caveats:** the full backend run had 286 passes and two arm-policy timing failures; both passed isolated reruns. The browser suite exposed a test setup disconnect race, corrected by selecting test modes before page load instead of opening an extra setup page. Placement/proximity and current-scene tests then passed. Two existing browser scenarios still fail on this machine under load: supervised arm policy can fault before the cancellation assertion, and the manual recharge test can miss its action-completion deadline. Their behavioral assertions were not relaxed; test-only startup/measurement timeouts were lengthened. A clean full browser regression run is still outstanding. Historical full-suite results below do not apply to this revision.
- **Capability gap:** these are still parking-trained weights. They can now attempt the selected navigation goal through the live UI, but the real recharge run did not route around the divider. Multi-challenge demonstrations, training and task-level evaluation remain necessary before claiming reliable completion of warehouse/recharge tasks. No planner, hidden target oracle or scripted base motion was substituted for SmolVLA.

## Live Local Navigation: 2026-09-11

- Added the opt-in **Local SmolVLA navigation** browser mode and **Local Model Parking** course. Start loads the native Windows CUDA checkpoint automatically, displays loading/warm-up progress in Agent interaction, and actuates the same worker streamed to the browser. No arm server or cloud model is used. The checkpoint, dataset and navigation safety bounds are unchanged.
- The course matches navigation validation case 15, seed 716. A fresh course is required; arbitrary scenarios/goals, chat, manually placed robots and previously moved episodes are rejected. Two preparation scans are explicitly scripted; subsequent velocity predictions are learned. All motion uses NavigationRuntime commands, matching observation tickets and the two-second freshness guard. Completion is scored after the model's two consecutive stop predictions, not used as a model input.
- **Real browser run:** session `4c2957fe-19c6-422a-bddb-6c160e35b91a`, episode `424640bc-0d5e-4fc9-bf9d-25288a5ba48d`, on the updated port-8002 app. Observed loading at 0 s, warm-up at 83 s, driving at 88 s, and completion at 120 s. **20 requests, 1.275 m travel, 23.6 simulated seconds, parking verified by physics, zero cloud tokens.** Head-camera bay pixels and nonblank WebGL pixels were checked. Native inference processes were absent after completion. This is one supported-course integration check, not a general navigation benchmark.
- **Scripted checks:** 23 focused backend tests passed for local loading, same-worker motion, observation privacy, camera restoration, unsupported-course rejection, and cancellation during loading/inference through Stop, takeover, reset, interaction-mode change and disconnect. Late replies cannot execute after interruption. Immediate Stop before the task starts clears loading without spawning; cancellation during subprocess creation retains the process handle for cleanup.
- Full browser suite: **37 passed**, including local course loading without cloud/arm configuration, real physics with scripted local predictions, takeover/reset, desktop/390px/320px layouts and canvas-pixel checks. Build, Pylance syntax and edited-file diagnostics pass. The full backend run had 281 passing tests and one outdated course-inventory assertion; that assertion was updated for `local_park` and passed its focused rerun. The added immediate-Stop test passed with the 23-test local subset. Existing dependency deprecations and the known Windows browser socket-close warning remain.
- Port 8002 was restarted while idle to load the new backend and the local parking course was then loaded explicitly through the UI. The old port-8000 app was not restarted. Previous CLI-only notes below are historical; `-NavigationTest` remains an optional isolated evaluation, while browser Start now loads its own local model. Luna + SmolVLA remains the separate left-arm mode.

## Arm Policy Offline Clarification: 2026-09-11

- Live diagnosis: app listening on 8000, no policy listener on 8085. The browser's `Luna + SmolVLA` readiness path requires seven-axis `milo-left-arm-v1` metadata; navigation checkpoints have two wheel outputs and remain CLI-only. Did not start an incompatible server, forge a readiness manifest, initiate motion or switch the user to cloud control.
- Clarified offline/incompatible/timeout messages and labeled the panel `Left-arm policy connection`, with explicit navigation incompatibility. Existing Start/Send readiness blocking and mode choices remain intact. Five focused backend readiness tests and one desktop/mobile browser workflow passed; build and edited-file diagnostics pass. Live HTTP checks confirm the new static panel is served on refresh. Backend API wording remains old in the existing process until restart; that process/session was not reset.

## Navigation Stopping Continuation: 2026-09-11

- Added opt-in `--balance-stops` to the navigation trainer. The 24/206 exact-zero training labels receive half the sampling probability and moving labels the other half. Neither labels nor normalization statistics change; validation/evaluation-only samples never enter the sampler. Default uniform training and manipulation behavior are preserved.
- Continued the original navigation weights for 500 actual CUDA updates (1,500 total), batch 4, new optimizer state. Drew 1,010/2,000 stopped samples. Fixed validation loss 0.49531 -> 0.31876; 244.6 s full run; peak allocated VRAM 3,320.7 MiB. Strict checkpoint reload passed. [Run report](../.runtime/navigation-stop-balanced-1500/report.json). No attribution to sampling alone: additional training is also a change.
- Diagnostic case 15 / seed 716 now parks and consistently stops in 20 requests; original run exhausted 30. This case motivated the change, so it is not independent proof. [Retest](../.runtime/navigation-stop-balanced-case15/report.json).
- Four disjoint, evaluation-only offsets were fixed before matched baseline/candidate runs (seeds 816-819, max 30 requests). Both pass 4/4 with no premature stops; command totals 81 -> 74, saturated requests 20 -> 27. Same worker, velocity limits, stop thresholds, setup scans and physics scoring. [Paired report](../.runtime/navigation-stopping-comparison/comparison.json). All first-run outcomes preserved. No general navigation success-rate claim.
- Added regression tests for sampler probabilities/invalid data, disjoint evaluation cases, paired seeds and preservation of simulated premature-stop failures. Final comparison summaries also include aggregate request/saturation totals and an explicit completeness flag. The original experiment reports are retained unchanged. No production controller, frontend, CLI default, live model selection, training dataset or prior checkpoint changed; live sessions were not reset. Screenshots/timelapse inspected.
- Full native Windows backend suite: **259 passed**, two existing dependency deprecation warnings, 270.67 s; `.runtime/navigation-stopping-regression.xml` retains the results. Pylance syntax and edited-file diagnostics are clear. No frontend source changed or browser test result is claimed for this training-only follow-up.

## Native Windows Navigation Fine-Tune: 2026-09-11

- Recorded 16 isolated real-wheel green-bay routes, including mirrored offsets and wide doorway posts. All recordings and saved-action replays succeeded; all 20 measured state values matched replay exactly. LeRobot export/readback verified every image and action/state vector: **276 samples at 1 Hz decision time**, 206 in 12 training episodes and 70 in four held-out episodes. Physics remains 240 Hz. Private geometry generates demonstration labels only, never model observations.
- Fine-tuned from the pinned public SmolVLA base for **1,000 CUDA optimizer updates**, batch 4. Navigation-specific 20-value state / two-value velocity action / one head-camera contract, chunk size 1, identity state/RGB normalization and training-only action statistics. Vision/language backbone frozen, 99,880,992 trainable parameters. Fixed validation loss 3.40054 -> 0.49546 (26 sampled held-out frames), peak allocated VRAM 3,323.9 MiB, 395.4 s complete run. Strict checkpoint reload passed. Separate `.runtime/navigation-training-1000/checkpoint/`; arm weights unchanged.
- Actual learned driving on held-out cases 12/13/14/15 (seeds 713/714/715/716): all reached a green bay; **3/4** met two consecutive learned stop predictions and physics parking checks. The fourth reached but oscillated and hit the request budget, retained as failure. Travel 1.00/1.11/1.25/1.62 m; 16/20/19/30 predicted commands. Initial inspect/locate scans scripted. Velocity saturation and stop deadband logged; worker clearance, acceleration, time/lease, ticket and Stop checks unchanged. No direct unvalidated wheel control, model waypoint oracle, Luna or cloud inference.
- Source, setup, limits and links to all reports/GIFs: [SMOLVLA.md](SMOLVLA.md#local-navigation-fine-tune). Tests cover recording corruption, wrong timebase/action order/embodiment, camera hashes, private field exclusion, exact replay, state mapping, finite outputs and saturation. Focused navigation + manipulation regression suite: **46 passed** in 62.40 s. Full backend suite: **256 passed**, two existing dependency deprecation warnings, 269.42 s; `.runtime/navigation-training-regression.xml` retains the results. Pylance syntax and edited-file diagnostics are clear. No frontend/production controller code changed, so no new browser-suite claim is made.
- This is a limited short-range docking experiment, not a trained multi-room planner, general obstacle-avoidance policy or live-UI model option. Single step and planning modes remain available and unchanged. Existing running sessions were not reset; no Linux/WSL, dependencies installed, uploads, or cloud calls. Broader starts/layouts and stable stopping remain future work.

## Windows Policy Pacing: 2026-09-11

- Added opt-in `PolicyTimedWorker` instrumentation and `benchmark_control --stage policy-timing` for small known-valid scripted trajectories with real physics/rendering and no inference. Baseline: 5/5 chunks expired after 28-31 of 96 ticks; requested waits ~4.2 ms actually took a median 18.9 ms. Median policy work was only 0.92 ms. [Baseline](../.runtime/policy-timing-before/report.json).
- `PolicyPacer` now uses `perf_counter()` absolute deadlines, compensates oversleep, and rebases on >50 ms lag or changed revision. Empty buffers reset pacing. No change to leases, freshness, physics timestep, Stop, speed, acceleration, collision checks, single-step or navigation pacing. After: 5/5 complete in 0.406-0.422 s within the original 0.55 s lease; all 96 ticks run. [After](../.runtime/policy-timing-after/report.json). Aggregate 230.2 Hz is not hard-real-time; p95 gap remains 17.5 ms.
- Repeated the three original real CUDA model-plus-adapter scene/seed cases with the same checkpoint and 10/20/40 request budgets. All **70/70 chunks complete, zero expiry**; policy physics time 4/8/16 s instead of 1.571/3.179/5.883 s. Maximum third-trial joint change 1.0469 rad; **0/3 pickups**, no assisted attachment. Exact chunk outcomes and optional worker metrics are now recorded independently of model inputs.
- [Held-out](../.runtime/milo-paced-heldout-1/report.json), [second](../.runtime/milo-paced-second-1/report.json), [third](../.runtime/milo-paced-third-1/report.json), [before/after image](../.runtime/milo-paced-third-1/comparison.png), [timelapse](../.runtime/milo-paced-third-1/motion.gif). Images inspected: arm approaches the cube but no lift. This is execution reliability evidence, not successful manipulation or raw-policy validation. No weights changed, cloud calls, production manifest or live-session reset.
- Focused regression suite: **56 passed** (policy, demonstration and benchmark tests), including simulated wait overshoot, long-stall/revision rebasing, actual valid trajectory completion, unchanged forced expiry and late-response rejection after Stop. Clean full backend rerun: **251 passed**, two existing deprecation warnings, 252.37 s; durable report `.runtime/policy-timing-regression.xml`. An earlier terminal run was interrupted after 250 passes and is not counted as a clean run. Three affected browser workflows (policy motion/cancellation on desktop/mobile, manual camera/Stop, navigation concurrency) passed in 28.7 s. No frontend source change or live-app restart.
- For the 10/20/40-request model trials, inference p95 was 0.618/0.581/0.561 s; maximum observed camera age at adapter dispatch was 0.688/0.703/0.672 s. Completion-observation p95 was 0.452/0.450/0.472 s (includes evaluator polling), all within the original chunk lease. These small fixed tests are not a general workload/latency guarantee. Editor diagnostics are clear.

## Advanced Training Grounds: 2026-09-11

- Review found that ordered transit counted without ground contact and final route completion stayed latched after departure. Fixed both, added optional simulation-time dwell, and added regression tests for airborne transit, motion, departure and repeated observations. Existing clinic route still passes; corrected its unsupported supply-carrying/striped-sign wording.
- Added Warehouse Dispatch Circuit (10 x 8 m, ordered checkpoints and 1 s final parking), Service Gallery Inspection (8 x 8 m, occluded target/red decoy and actual-camera dwell), and Cluttered Assembly Workshop (two-arm lift/release/settle among fixed fixtures). All three feasibility tests pass through real wheel/joint/gripper commands. Warehouse route >20 m; inspection route >15 m; workshop completes both cubes without fixture displacement. Scripted mechanics only, not model capability or policy-training results.
- Catalog now has ten presets plus practice. Navigation/Perception/Manipulation groups and Advanced tags preserve the existing UI. Large facility framing derives from actual floor geometry, including mobile. New floor dimensions and private objective geometry are excluded from model observations; text and voice privacy coverage includes the new scenes.
- Full Windows backend suite: **247 passed**, two existing dependency deprecation warnings, 242.48 s. Build passes (existing large-bundle warning). Three focused Edge tests pass: all catalog scenes load/reset, advanced scenes render nonblank textured canvases at 1440/390 px, no horizontal page overflow, and real warehouse motion updates spectator/head camera and resets to zero time. Captured desktop/mobile scene and authoritative-camera screenshots inspected.
- Final full Edge suite: **35 passed** in 3.3 minutes. Initial full/isolated runs exposed a pre-existing race in the navigation overlap test's separate HTTP reads. The test now records advancing physics timestamps from live WebSocket frames within the same inference turn/revision; production timing is unchanged. Two Windows socket-close callbacks occurred during final teardown, with all assertions passing. Edited-file diagnostics are clear.
- A fresh preview on `http://127.0.0.1:8000` serves the updated build with Warehouse Dispatch Circuit loaded. Verified ten catalog entries, four Advanced entries, page/camera HTTP 200, controller inactive with zero turns and no error. No previous listener on ports 8000-8010 was running or stopped.
- These are deterministic rigid-body practice scenes, not randomized environments or automatic training data. Current SmolVLA policy remains experimental, left-arm-only and not validated on these grounds. No cloud model was called, weights trained or existing user episode reset for this task.

## Approved Adapter-Assisted Motion: 2026-09-11

- Continued the 100-step checkpoint for 1,000 updates with batch size 4, new AdamW state and the same four/one episode split. Fixed held-out loss 0.73946 -> 0.05972; 557.7 s total, 3,321.5 MiB peak allocated VRAM. Strict checkpoint reload passed. Original weights/reports preserved.
- Three raw-policy trials with fixed cube/seed pairs still rejected their first chunk, with no learned motion. User explicitly approved an opt-in isolated trajectory adapter. It uses the model's index-19 target, logs saturation to real joint bounds, and produces bounded 0.4 s quintic trajectories from rest. Original tickets, freshness, collision, Stop and worker rate limits remain unchanged. No production code or readiness manifest was changed.
- Three adapter-assisted trials accepted 10/10, 20/20 and 40/40 targets. Maximum arm changes 0.1133/0.2459/0.2446 rad; third-trial gripper change 0.0537 m. Each produced measured and visible motion. **Zero of three lifted or placed the cube.** Repeated wall-clock chunk expiry truncated motion; acceptance is not complete trajectory execution.
- Third-trial camera comparison showed 2.88% changed pixels. [Before/after](../.runtime/milo-adapter-third-1/comparison.png), [41-frame timelapse](../.runtime/milo-adapter-third-1/motion.gif) (250 ms/frame, not real-time), and [report](../.runtime/milo-adapter-third-1/report.json). Model receives only paired head image/sensors plus instruction; evaluation geometry is private.
- **43 demonstration/training/adapter/policy tests passed** in 38.38 s. Pylance syntax and editor diagnostics are clear. Windows-only, no cloud calls, no uploads, no live-session resets. This is repeatable model-plus-adapter movement, not raw-policy or autonomous task success. See [SMOLVLA.md](SMOLVLA.md#adapter-assisted-movement).

## Native Windows Training And Candidate Evaluation: 2026-09-11

- Trained the cached pinned SmolVLA action expert for 100 steps with Milo's seven-axis state/actions and single head image. Four episodes (2,880 frames) train; the fifth episode is held out. Normalization uses training episodes only. No uploads or cloud calls.
- All losses and gradients finite; eight fixed held-out loss samples improved 1.02030 -> 0.73974. Peak allocated VRAM 3,320.2 MiB, complete run 114.8 s. Strict checkpoint reload produced finite [1,50,7] outputs. This small nearby-position split does not establish generalization.
- Real CUDA prediction in an isolated `SimulationWorker` took 0.409 s. The unmodified chunk contained 39 gripper openings above the 0.11 m limit (maximum 0.2417 m), so `PolicyChunk` rejected it before learned motion. Arm/gripper state unchanged, zero policy physics time, no lift or completion. Camera setup is separate from learned motion.
- Saved processor action round-trip error <=1.19e-7. No production manifest, live-UI enabling, safety-limit changes, scripted replacement actions, or existing-session resets. See [SMOLVLA.md](SMOLVLA.md#windows-training-and-local-test) for reports and repeat commands. Earlier CPU-only/no-training entries below are historical.
- **40 demonstration/training-contract/policy tests passed** in 39.47 s; edited Python files have no editor diagnostics. A repeat with the final evaluator again rejected the first chunk and preserved all seven arm/gripper values, then shut down its worker and CUDA subprocess. [Final evaluation report](../.runtime/milo-policy-evaluation-final/report.json). A completed test process with a safety rejection is not task success.

## Pretrained SmolVLA CPU Smoke: 2026-09-11

- Added a non-actuating probe, `scripts/probe_smolvla.py`. Downloaded the pinned public base checkpoint `c83c3163b8ca9b7e67c509fffd9121e66cb96205` and strictly loaded all policy keys. Backbone config/tokenizer revision is `7b375e1b73b11138ff12fe22c8f2822d8fe03467`. A Windows long temporary-download path caused the initial failure; shortening local cache names resolved it.
- Real CPU float32 inference succeeded for one warm-up and two repeats: **5.55 / 5.32 / 5.54 seconds**, respectively. Each output is **[1,50,6], finite**. Model parameter count 450,046,176; eight CPU threads; original 10 denoising steps. Peak process RAM **2,620.8 MiB**, load 51.61 s, download 14.96 s. Report and full action values are in `.runtime/smolvla-base-smoke-run2/report.json`.
- Inputs deliberately use synthetic six-value zero state and the same saved Milo head image in the three declared camera slots. The instruction is 7 tokens within the 48-token cap. These are shape-compatible test inputs, not calibrated multi-view observations or proof of visual grasp understanding. Repeated inputs, only two post-warmup samples and CPU-only execution preclude broader performance claims.
- No actions were executed, no app sessions were changed, no fine-tuning occurred, and the production trained-checkpoint gate remains in place. The six-value base output is not mapped to Milo's seven-axis action space. CPU latency exceeds the current one-second freshness limit. **9 probe tests passed**, including shape/finite-value validation, with clean editor diagnostics. GPU performance and learned Milo task success remain unverified.

## Real LeRobot Dataset Export: 2026-09-11

- Installed LeRobot 0.6.1 dataset/training extras through the explicit CFS Python feed in `.runtime/smolvla-env`. Exported the five validated physical-only recordings with the real LeRobot writer to `.runtime/datasets/milo-pilot`. No uploads, model weights, training or changes to running robot sessions.
- Reopened all **3,600 samples across five 720-frame episodes at 20 Hz**. Pixels match source frames exactly; state/actions equal the float32 conversion of raw values (maximum representation error **5.96e-8**). Task strings, timestamp/frame/index alignment and exclusive episode boundaries match. Fifteen 50-action training windows verify last-action padding and masks without cross-episode leakage.
- Vector min/max/mean/std statistics match LeRobot's source recomputation. Small differences from direct float64 std, at most 3.57e-5, are consistent with LeRobot's float32 second-moment calculation; explicit tolerances and per-axis values are retained in `.runtime/milo-export-validation.json`. No statistics were overwritten. Image statistics were checked for finite per-channel shape and [0,1] range; LeRobot computes them from sampled images.
- Added `--verify-export` and corruption regression tests to the existing dataset tooling. **17 demonstration/export tests passed** in 26.47 seconds. This replaces the earlier mock-only export status, not the unverified training/inference status.
- TorchCodec could not load native libraries and export selected LeRobot's PyAV fallback; explicit PyAV readback works for this image-backed dataset. No video decoder claim is made. GPU PyTorch, broader demonstrations and a trained checkpoint remain outstanding.

## Approved Python Feed: 2026-09-11

- Found the machine-wide pip index at `https://packagefeedproxy.microsoft.io/pypi/simple/`. uv does not read pip's configuration, so prior uv installs missed this existing approved route. Added the exact feed to `scripts/requirements-smolvla.txt`; no global configuration or production environment was changed.
- Installation through CFS succeeded: 64 runtime packages installed, including LeRobot 0.6.1, Transformers 5.5.4 and PyTorch 2.11.0+cpu. SmolVLAPolicy and the local policy server adapter import; `uv pip check` reports consistent dependencies. No TLS bypass, public-registry fallback, model inference or robot motion occurred.
- PyTorch has no CUDA build and reports `cuda.is_available() == False`. GPU support requires an approved CUDA wheel source, not a restart. No CUDA-specific build was identified in the CFS PyPI version listing. Dataset/training extras and a Milo-trained checkpoint are still outstanding. Earlier installation-blocked entries below are historical; this entry supersedes their package-installation status only.

## Physical-Only Demonstration Pilot: 2026-09-11

- Added isolated worker-owned recording and numeric-action replay in `scripts/record_milo.py`, strict raw-data validation and a local LeRobot writer adapter in `scripts/milo_dataset.py`. No production control/physics limits were changed. The demonstrator uses privileged IK target positions; exported model features contain only paired head RGB, measured state, commanded action and task.
- **5/5 recordings and 5/5 fresh-worker replays succeeded** across the five fixed, narrowly varied cube starts. Each episode has 720 frames at 20 Hz simulation time, for **3,600 frames / 180 simulated seconds**. Cube-bottom lift peaked at 0.1996-0.2021 m with both fingers in contact. No Bullet constraint/assisted attachment was used; cubes were released and grounded inside the target square at rest. Seven-axis pre-action state replay error was **0.0** for every episode. Source/terminal head-camera samples were inspected.
- Data and summaries are under `.runtime/milo-demonstrations-pilot/`; all frames passed schema, timing, finite values, camera hash/size/path and replay-evidence validation. Early rejected trajectory probes are stored separately. The recorder uses an explicit offline simulation clock; no wall-clock inference, continuous visual reaction, or trained-policy success claim is made.
- **27 demonstration/policy tests passed** in 49.78 seconds, including ten new recorder/export/physical-grasp tests. The LeRobot adapter was mock-tested against its official writer contract; actual LeRobot-format materialization is unverified because installation remains blocked. No training or cloud inference occurred, and running user sessions were not altered.
- Network diagnosis reproduced the package-host TLS failure with uv, Windows curl and PowerShell, for metadata and the actual LeRobot wheel. PyPI, Hugging Face and the CUDA torch package index are reachable. No Windows explicit proxy/PAC was configured; interception/certificate root cause remains undetermined. Exact artifact and support diagnostics are recorded in [SMOLVLA.md](SMOLVLA.md). No TLS or policy bypass was attempted.

## SmolVLA Readiness Gate: 2026-09-11

- Diagnosed the reported port-8003 error: zero policy listener on 8085, no manifests in `.runtime/checkpoints`, and neither LeRobot nor PyTorch in the isolated policy environment. The session had zero supervisor turns and no motion. Missing runtime/trained weights remain unresolved; no base or scripted policy was substituted.
- Added a non-inference readiness API and UI check with offline/incompatible/untrained states, retry, and disabled policy Start/Send controls until a compatible service responds. Direct starts recheck before session creation; Stop/operator changes during preflight reject the late start. Existing Single step and Navigation plan remain available.
- **45 policy/API tests passed**, with two existing dependency deprecation warnings. **Two focused Edge tests passed** for offline gating, no model calls, normal-mode availability, retry, endpoint invalidation, mobile layout, and the existing scripted-policy real-physics workflow. Build and diagnostics pass. Real SmolVLA execution remains blocked on installation and a Milo-trained checkpoint.

## Request Context Indicator: 2026-09-11

- Added per-request `agent.context_usage` for the retained-history estimate/budget, retained turns, images sent/limit, command resent, and actual provider input count. It is reset on session start and before each response; cumulative run token totals remain separate. No model-window size or image-token cost is guessed. History trimming and default settings are unchanged.
- Existing zero-history tests verify the current chat command is still supplied on every step; per-request estimates are checked against the submitted feedback trace, and pending input counts remain unknown instead of reusing earlier tokens. The supervisor's instructions and current goal are outside the history budget; prior chat follow-ups are not permanently pinned.
- Focused browser tests passed for actual scripted request telemetry, budget-versus-input distinction, zero history, pending/missing usage, settings edits, episode reset, offline labels and 1440/390/320 layouts. Screenshots were inspected. These are scripted tests, not a live-model memory benchmark. Build passed with the existing bundle-size warning.

## Luna + SmolVLA Integration: 2026-09-11

- Added `supervised_policy`, a cloud supervisor's five allowlisted skill tools, an independent single-flight local policy runner/server adapter, and worker-owned bounded left-arm/gripper execution. Production requires declared Milo training plus exact state/action/camera compatibility; no base-model or scripted fallback is enabled.
- A bounded snapshot renderer process removes continuous policy-mode camera rendering from the physics thread. The test suite explicitly replaces that thread's camera renderer with a failing function while policy motion and paired image feedback continue. Default single-step/navigation paths are unchanged.
- Full backend regression suite: **197 passed**, two existing dependency deprecation warnings, in **232.60 seconds**. Thirteen focused policy tests cover real arm motion during supervision, contracts, unsafe and stale chunks, completion provenance, independent Stop, timeout and inference failure, snapshot shutdown and manual-feedback restoration.
- The final full Edge run passed **29/30 tests**; the existing navigation test missed its short moving/thinking overlap window. That test passed on an isolated rerun without code changes. The new desktop/mobile workflow passed in both full runs and its focused run: policy traces, canvas pixels, 320x240 camera delivery and label, no horizontal overflow, cancellation, and reset. Both Realtime voice tests passed in the final full run. An earlier full run was interrupted during a terminal transition and is not counted as a clean pass. The build passes with the existing bundle-size warning; one Windows socket-teardown callback was also logged. See [SMOLVLA.md](SMOLVLA.md) for setup and remaining validation gates.
- Real SmolVLA installation in an isolated Python 3.12 environment was blocked by TLS `HandshakeFailure` fetching the published LeRobot 0.6.1 wheel metadata, including a Windows trust-store retry. No trust checks were disabled. No Milo demonstration dataset/checkpoint was created, no real SmolVLA inference or new live Foundry call was performed, and no post-change 100 Hz or autonomous manipulation performance is claimed.
- Updated app started separately at `http://127.0.0.1:8003`; existing port-8000 and port-8002 processes were not restarted. HTTP 200, the new API execution mode, exact five supervisor tools, local endpoint default and visible checkpoint-required UI were checked. No model inference was triggered by that live UI check.

## Image Batches and Context Budget: 2026-09-11

- Chat and navigation accept `images_per_request` from 1 to 8 (default 1) and `context_tokens` from 0 to 32768 (default 4096). Frames are distinct real paired head-camera observations, current first, with historical sensors/time explicitly labeled. The charging reference counts toward the selected image limit. No spectator data or extra motion is introduced.
- Complete text turns and action summaries are retained within a conservative UTF-8-byte token estimate, with a 200-turn object guard. Images are retained separately and not replayed through old text turns. Zero clears text history while preserving the active request/current feedback. Selected camera pairs and mandatory current inputs are outside this retention budget; actual provider token usage is still reported separately.
- **179 backend tests passed**, with two existing dependency deprecations, in 164.75 seconds. Coverage includes real three-frame batches, uniqueness and limits, zero history, active chat intent, complete tool pairs, retention beyond six turns, charging-memory bounds, image URL lifetime/reset, and native Ollama multi-image payload/context sizing. Existing navigation, Stop, takeover, and voice checks pass.
- Build and editor/Pylance syntax checks passed. **29 Edge tests passed** in about 2.8 minutes; typed follow-ups and autonomous starts submit both settings, the feed loads the exact historical image, and desktop/mobile controls and gallery layouts were checked. No new live model inference or model-quality benchmark was run.
- The inactive port-8002 app was restarted after saving public diagnostics/settings under `.runtime/diagnostics/before-context-update-20260911-122710.*`. The updated UI and API limits were verified, all four model profiles restored, and a fresh Apartment Search episode left idle. Realtime voice conversation retention is unchanged.

## Motion Buffer Recovery: 2026-09-11

- Reproduced the reported `BUFFER_EXPIRED` failure with a deterministic clock: rendering/physics slower than wall time could expire a buffer despite fresh local feedback. The public Nano trace also showed a 1.2-second scan with a 1.7-second lease and a preceding 2.797-second response.
- Expiry still brakes, discards pending motion, and invalidates revisions, but now preserves the active skill for fresh-feedback recovery. Refill timing considers wall-clock lease and motion remaining; models whose observed latency cannot fit plan from rest. Revision changes cancel and await outdated requests before new inference. Stop now immediately publishes the cleared buffer.
- All **172 backend tests passed**, including 15 navigation tests, with two existing dependency deprecations. All **29 Edge tests passed** against isolated real physics and scripted inference. Editor diagnostics were clean. New tests cover slow simulation, expiry/exhaustion during inference, no overlapping requests, successful fresh-feedback updates, slow-model fallback, and preserved hard safety stops.
- The user-authorized port-8002 restart restored public provider profiles and a fresh Apartment Search scenario. Page and head camera returned HTTP 200; the controller was idle with no error. Prior public state/trace/camera were preserved under `.runtime/diagnostics/before-buffer-recovery-20260911-115822.*`.
- No live model inference was started for validation. These tests establish controller recovery, not autonomous navigation success or continuous motion with a slow model. Buffer pauses do not extend skill/session deadlines or relax collision, feedback-loss, or Stop checks.

## Reactive Navigation: 2026-09-11

- Implemented opt-in `navigation_plan` execution for both Foundry and Ollama, with ordered inspect/locate/approach/cross skills, model-reviewed completion checkpoints, a two-second replaceable motion buffer, and live plan/buffer/cancellation UI. Single-step remains the default, and Realtime voice retains its original tools.
- Full backend suite: **160 passed, 2 third-party warnings in 159.70 s**. Navigation checks cover a complete four-skill traversal through real doorway geometry, continuous drive velocity across replacement, in-flight worker updates, bounded horizons, scan/movement preconditions, stale/expired revision rejection, floor clearance, buffer and feedback expiry, skill timeout, and inference cancellation after a worker fault.
- API checks exercise Stop, takeover, reset, interaction-mode switching, and last-operator disconnect during buffered motion. Pending motion is discarded, existing completions remain distinguishable from cancelled steps, and manual placement is rejected during navigation ownership. Both model adapters are tested with their actual mode-specific request schemas.
- Production build and editor/Pylance diagnostics pass. Full Edge suite: **29 passed in approximately 2.8 minutes**. The navigation fixture verifies skill progression, motion while inference is pending, canvas pixels on desktop/mobile, buffer cancellation, and reset back to single-step mode. Desktop/mobile screenshots were inspected. The existing bundle-size warning remains.
- These are scripted-policy and real-physics checks, not live model recognition or autonomous navigation results. The model selects doorway semantics and supplies visual evidence; mechanical checkpoints do not verify that claim. Slow inference can exhaust a buffer and pause the robot. Constant-velocity lookahead and software watchdogs are conservative simulator safeguards, not certified real-hardware safety or a general motion planner. No cloud resource or model deployment was created.

## Feed Export and Stalled Driving: 2026-09-11

- The inspected live problem trace already replayed six action/result pairs. Later forward commands requested 0.1 m/s for 1 s, reported `ok`, and produced only about 0.000007-0.000052 m encoder displacement with front contacts around 64-67 N. Reported input sizes were about 13.4k tokens, below the configured 16,384-token context; this does not independently prove that the provider used every history item. The public diagnostic trace was preserved at `.runtime/diagnostics/repeated-forward-feed.json`.
- Contact-backed stalled drives now return `NO_PROGRESS` with inspection/replanning guidance. Real apartment physics verifies the error, normal head inspection, zero-motion commands, and retreat. Scripted model tests verify the last-three-action summary reaches the actual request and trace once, and three consecutive stalled drives stop without automatic wake. No hidden object/room geometry is added to feedback.
- **Copy exchange feed** exports all loaded retained events, irrespective of filters or collapsed payloads, with revision/truncation metadata and session-local camera URLs (not embedded images). The Edge clipboard test uses a clipboard shim to verify full payload equality, failure/retry, disabled empty state, reset, and mobile layout. It does not independently validate OS clipboard permission policy on other browsers.
- Full backend suite: **141 passed, 2 third-party deprecation warnings in 222.74 s**. An initial run exposed an existing completion-publication race; publishing `busy=false` atomically with the final observation fixed the isolated failing check and the full rerun.
- Frontend production build, editor diagnostics, and full Edge suite passed: **28 tests in approximately 2.8 minutes**, with real physics and scripted inference. The existing bundle-size warning remains. The mobile copy-control capture was inspected.
- These results establish export correctness, clearer feedback, and bounded repeated pushing. No new live model run or autonomous recovery/navigation success is claimed.

## Ollama Integration: 2026-09-11

- Gemma 4 E2B (`gemma4:e2b-it-qat`) is selectable alongside Luna, Nano, and legacy Foundry profiles. Ollama uses an independently validated HTTP loopback endpoint and `think=false`; local sessions never construct the Foundry adapter or forward cloud credentials.
- Full backend suite: **138 passed, 2 third-party warnings in 121.64 s**. Added native image/tool conversion, paired tool-result history, private-reasoning exclusion, incomplete/invalid/multiple-call rejection, missing-model/offline messages, actual worker movement, and pending-HTTP cancellation checks.
- Follow-up adapter checks: **11 passed in 6.51 s**, including preservation of native Ollama call IDs and a real-physics check that a repeated ID cannot repeat motion.
- Production build and editor diagnostics pass. Full Edge suite: **27 passed in approximately 2.6 minutes** with scripted inference and real physics. It verifies local configuration edits, None reasoning, Stop, endpoint independence, preservation of Foundry choices, and use without a cloud endpoint. Desktop/mobile full-page captures were inspected. The existing bundle-size warning remains.
- A separate **live Ollama** test used fresh kitchen camera/sensor observations and the production controller in an isolated physics instance. Gemma called `set_head` with yaw -0.3, pitch 0.2, duration 1 s, then `stop`; both returned `ok`, the measured head reached the target, and the camera changed. Inference took **12.469 s** then **0.782 s**, with 10,043 input and 46 output tokens reported across two requests. The run did not contact Foundry or alter the operator episode. Local report: `.runtime/ollama-app-check.json`.
- This verifies integration and one explicit head-control task, not autonomous room navigation, grasping, continuous control, or hybrid Luna supervision. Earlier simple dry runs showed direction mistakes despite schema-valid calls; retain conservative goals and operator Stop.

## Recovery Check: 2026-09-11

Recovered the compatible API, physics worker, sensor contracts, robot geometry, frontend, and regression tests from VS Code local history after earlier mechanical-only files replaced them. Each replaced file was backed up under `.runtime/recovery-20260911/` and both backup and restored contents were hash-checked. Credential files and the surviving Foundry model configuration were not changed.

- Full backend suite: **121 passed, 2 third-party deprecation warnings in 119.61 s**.
- Frontend production build and dependency consistency check passed with Vite 7.3.6 and Playwright 1.55.1. The existing bundle-size warning remains.
- Full Edge suite: **25 passed in approximately 2.3 minutes**, using real physics and scripted inference on isolated port 8001. One Windows socket-teardown connection-reset callback was logged; all assertions passed.
- Desktop/mobile apartment screenshots and canvas-pixel checks verified the restored 3D view, head camera, room fixtures, sensors, and outcome feedback.
- Luna, Nano, and configured Foundry alternatives remain available. No local SLM is wired into the app or invoked by this recovery. The standalone local-model probe remains separate and unchanged.
- No live model invocation or autonomous task-performance claim is part of this recovery. The sections below retain their original historical validation details.

Date: 2026-09-10 (historical results below). Mechanical, LLM, Realtime voice, and six predefined challenge slices have passing local tests. A live GPT-5.2 image/sensor/tool probe passed; GPT-5.6 Luna, GPT-5.4 Nano, and live Foundry Realtime deployment access, physical microphone quality, and autonomous task results remain unverified. Randomized challenge benchmarks and experiment persistence remain unimplemented; Ollama was added in the integration check above.

## Verified with Real Physics

The Python tests run against conda-forge PyBullet 3.25, Python 3.11.16, NumPy 1.26.4 and SciPy 1.17.1 on Windows. They do not substitute mocks for mechanics.

- Both six-joint arms reach the curated floor target through `move_end_effector`.
- Both arms have fixed shoulder brackets bridging the torso and the unchanged shoulder pivots. Asset tests verify visual/collision overlap at the torso and pivot; desktop/mobile captures show the connected assembly.
- Both arms close on actual opposing finger contacts, establish an assisted grasp, lift the cube, release it, and allow it to settle under gravity.
- Closing far from a cube does not establish a constraint.
- Unreachable IK targets fail without advancing physics.
- Collision-rejected arm targets do not move.
- Differential wheel driving translates and rotates the base; a wall blocks translation.
- Head yaw/pitch change the authoritative frame and achieve the tested target angles.
- Agent observation top-level serialization excludes evaluator bodies, objects, depth, goals and exact robot position.
- Stale observation sequences, duplicate action IDs, stopped runs, and obsolete episode commands do not cause unintended extra motion.
- A stop event interrupts a two-second worker command before completion.
- Reset replaces the run ID/epoch, disposes the previous clients, and rejects obsolete live-frame access.
- Manual HTTP endpoints return real camera PNGs and route motion through the validated simulator.
- Cross-origin mutation requests are rejected.

The full backend suite, `./.runtime/env/python.exe -m pytest -q`, completed with **117 passed, 2 warnings in 173.53 s**, including Kitchen to Bathroom, Luna/Nano configuration, textures, proximity sensors, structured outcomes, idle/wake lifecycle, Chat/Voice modes and follow-ups, Apartment Search, manual placement, in-flight live previews, return-to-charge energy/memory, predefined challenges, shoulder attachment, LLM controller, API, exchange-feed, and Realtime voice tests. Before LLM integration, the mechanical suite had 12 passing tests. Test-suite wall times are not simulator FPS measurements or model performance claims. CPU camera throughput has not been profiled.

The root launcher `./start.ps1 -SkipBuild -Port 8000` was run successfully after building the frontend. It serves the operator interface at http://127.0.0.1:8000 and API documentation at http://127.0.0.1:8000/docs.

The HTTP test emits third-party Starlette/httpx and AnyIO deprecation warnings. It passes; these dependencies should be reviewed before a release.

## Verified in Microsoft Edge

- `npm --prefix frontend run build` passes with TypeScript 5.9.2 and Vite 7.3.6.
- `npm.cmd --prefix frontend test -- --reporter=dot` passes with Playwright 1.55.1: **25 passed in approximately 2.9 minutes** against an isolated PyBullet backend on port 8001 with test-only scripted inference, including the sixth scenario and Luna/Nano controls. The Windows test server logged two connection-reset callbacks during socket teardown; all assertions passed.
- Desktop (1440 x 1000): nonblank spectator canvas, visible robot, orbit-driven pixel changes, and unchanged authoritative head-camera reference during spectator orbit.
- Manual forward driving changes the authoritative frame and increases measured forward odometry by more than 0.1 m; head commands, stop, and resume update the interface and timeline.
- Mobile (390 x 844): no horizontal page overflow with arm/gripper controls open, nonblank spectator pixels, and a decoded 640-pixel-wide head-camera image.
- Tests reset the episode independently. Full-page desktop/mobile screenshots were captured and visually inspected.
- LLM controls: default Luna/low/2-second settings, disabled unconfigured start, connection editing, adding/selecting deployments, goal submission, real scripted-tool motion, live rate updates, manual takeover, and Stop.
- The LLM connection panel and controls fit the 390-pixel viewport. Dedicated desktop/mobile screenshots were inspected. Browser tests never send inference requests to the user's Foundry project.
- The sticky activity strip distinguishes executing commands from model thinking and feedback waits in real-physics manual/scripted-LLM tests. A separate UI-state fixture covers stopped/stopping, completed, error, voice, disconnect, and reconnect precedence. Reopening the WebSocket alone cannot restore a running label before fresh state arrives. The clock is outside the announcement region, and reduced-motion preferences disable animation. Desktop, 390-pixel, and 320-pixel screenshots were inspected; the strip stays visible while scrolling without horizontal overflow.

The browser stop check verifies the stopped/resumed UI states. In-flight interruption is covered separately by worker and agent tests. Vite reports a non-blocking bundle-size warning (approximately 760 kB minified / 207 kB gzip); code splitting has not been tuned.

## Materials, Sensors, Outcomes

- Six generated bitmap textures have verified dimensions and pixel variation. Removing textures changes the authoritative camera pixels while leaving physics snapshots unchanged. API tests verify shared material delivery and reject unknown asset names. Edge confirms all six materials load, renders nonblank desktop/mobile scenes, and reports no page errors. Textured apartment screenshots were inspected.
- Real ray tests verify the expected distance to a known wall, correct side readings after base rotation, null clear readings, and no object identity/pose leakage. Real wheel motion produces directional contact forces; normal floor contact does not trigger bumpers. Browser readings update during motion independently of model feedback.
- Structured-outcome tests distinguish agent-reported completion/unachievable from physics verification and retain reasons through interruption. Real scenario browser tests show verified completion and operator-assistance labels. Controlled UI tests cover failures, errors, limits, interruptions, normal replies, blocked actions, newer-action priority, reset clearing, and disconnect overriding stale success.
- Result feedback remains visible while scrolling and during idle; 1440-, 390-, and 320-pixel captures were inspected. All validation uses real local physics, scripted inference, and synthetic voice input. No live model success or real-hardware sensor/safety guarantee is claimed. A shared-terminal interruption during the first full browser run was resolved by rerunning the entire suite in isolation.

## Idle and Wake Evidence

- Camera detector tests verify the five-second stability boundary, small-noise tolerance, meaningful-change revision, and real head-camera changes without stepping physics or incrementing model observation sequences.
- A real five-second controller test verifies no new inference or observation during idle, then one camera-triggered wake with fresh input and preserved session usage. Additional tests cover structured unachievable outcomes, real parking completion stopping continuation, pending static-camera inference remaining active, turn-limit enforcement, and stale stop-revision refusal.
- API tests verify Stop, takeover, mode switch, reset, and final disconnect cancel the watcher and clear automatic waking. Voice tests cover camera wake with `tool_choice=none`, no automatic recording, new push-to-talk wake, and watcher cleanup on halt.
- Edge tests verify visible idle status, stable tokens, unchanged observation sequence while idle, spectator axes not waking the model, head-camera motion waking the same text session, Chat interaction wake, and Voice idle with microphone off. Desktop/mobile idle screenshots were inspected. All inference is scripted, all robot motion is real local physics, and microphone input is synthetic.
- The feature intentionally requires a finished/unachievable task plus five quiet seconds. It does not infer task impossibility from stillness or cancel a slow model request. The detector's tolerance and 0.5-second sampling are heuristics; tiny or shorter-than-sample scene changes can be missed. Idle monitoring does not make physics free-running, open the microphone, reset request budgets, or establish autonomous model performance.

## Chat and Voice Evidence

- Controller tests exercise a no-motion text answer followed by a movement request and tool-result continuation. Follow-ups retain prior text and paired robot feedback in bounded history; model inputs exclude spectator/evaluator state. Empty messages, concurrent requests, and stale conversation IDs are rejected.
- API tests verify Chat defaults, explicit mode selection, operator-connection and same-origin guards, text/voice exclusivity, cancellation of pending chat on switching, and reset invalidation. A late voice start cannot activate while Chat is selected.
- Edge tests send typed questions and follow-up movement commands through real physics, verify user/assistant transcripts and the latest reply's visibility, and switch away from an intentionally pending response. Keyboard tab switching retains focus. Voice tests explicitly choose Voice and verify switching to Chat while recording releases microphone tracks and ends server ownership. Mode selection alone does not request microphone access.
- Desktop/mobile Chat and Voice screenshots were inspected. Existing goal runs, voice replies, token counters, challenges, live views, and manual controls pass the full browser suite. Tests use scripted inference and synthetic microphone input, not a new live model conversation. Chat and Voice do not share their model context; transcript persistence beyond the current controller/episode is not implemented.

## Token Tracker Evidence

- The sticky tracker uses the existing server totals, showing input, output, and their sum for a text or voice session. Text fixture checks verify 10 input + 5 output = 15, unchanged while another response is pending and retained after takeover. The two-response voice fixture verifies 20 + 10 = 30, retained after End voice and reset on the next voice run. Episode reset clears the display.
- A controlled WebSocket fixture verifies exact digit grouping, current/last/no-run scope, large counts, and retention marked `Last received` after disconnect until fresh state arrives. The tracker is outside the robot-status live region. Desktop, 390-pixel, and 320-pixel screenshots were inspected; counts fit and remain visible while scrolling.
- This is frontend tracking of provider-reported response usage. Missing usage or interrupted requests are not estimated; the UI tooltip and README disclose that these totals are not a billing ledger. Each Chat message starts a bounded control run and resets these counters, while the conversation transcript retains follow-ups. No paid model request was made for tracker or mode validation.

## Manual Placement Evidence

- Worker tests verify a new physical position and camera image, preserved heading/height, unchanged simulation time/odometry, one fresh observation, and an incremented manual-placement count. Stale, out-of-floor, obstacle-overlapping, and stopped requests leave the authoritative pose unchanged. Stop cancels a placement already queued behind another worker operation.
- Existing real-grasp tests for both arms reject repositioning while carrying an assisted cube, retaining the original pose, observation sequence, and grasp.
- API checks reject nonfinite coordinates, wrong dimensions, extra fields, cross-origin requests, stale episodes, and both text/voice ownership conflicts. Reset clears placement history. No placement tool is exposed to models.
- Desktop mouse and mobile native-touch tests select the rendered robot and drag it. Before release, the scene state and camera remain unchanged; after release, the real pose changes, the camera refreshes, and the footer shows manual intervention. Keyboard tests cover preview cancellation, blocked-drop rollback, successful placement, Stop, and reset. Selection/preview and applied-placement screenshots were inspected.
- These are operator-editing checks, not autonomous navigation results. Background orbiting and existing live camera/model/voice workflows still pass.

## Live Preview Evidence

- A wall-paced two-second head movement produces multiple distinct PNGs and changing pose snapshots before completion. Intermediate previews do not increment the model observation sequence or enter its frame cache; the final preview reuses the final observation image. The preview cache is bounded and cleared on close.
- API checks verify initial/final PNG delivery, `no-store`, preservation of model-observation images, and rejection of old-run or missing preview URLs after reset.
- Desktop and mobile Edge tests execute a two-second wheel movement requested by the scripted LLM. At least three decoded camera frames appear while the controller is still acting, with changing head-image and spectator-canvas pixels and increasing simulated timestamps. Model observation sequence remains constant during motion and only one model turn has occurred.
- The mobile test delays camera responses by 180 ms, verifies at most one preview request in flight, and still observes motion before completion. Both tests receive the final frame and a fresh zero-time image after reset. In-flight desktop/mobile screenshots were inspected.
- These are real-physics/scripted-inference checks, not a new live Foundry run or a measured 10-fps guarantee. Model feedback intervals are unchanged; physics still holds between commands.

## Predefined Challenge Evidence

- Kitchen to Bathroom: a real-wheel route leaves the kitchen through its doorway, crosses the hall, enters the bathroom, and stops with over 4 m of measured travel. The initial pose is in the kitchen without collision and with zero odometry. Rays verify the dividing wall blocks a direct route and both doorways are open. Arrival checks reject the kitchen, hall, partial doorway occupancy, motion, and lack of floor contact; leaving the bathroom clears completion. Camera bytes change, and the upper toilet fixture is visible from the tested arrival pose.
- The sixth preset's browser check verifies loading, the room-identification goal, 100-turn default, arrival feedback, and reset to the kitchen. Its completion-display step uses recorded manual placement plus a physical wait; wheel-route feasibility is tested separately. Inspected captures under `frontend/test-results/` include `kitchen-start-camera.png` (fridge, sink, oven), `bathroom-arrival-camera.png` (toilet, bathtub, basin), and `kitchen-bathroom-desktop.png` / `kitchen-bathroom-mobile.png`. Both viewports have nonblank spectator pixels and mobile has no horizontal overflow. Text/voice tests exclude private initial pose, fixture names, room labels, arrival geometry, and progress. No live model room-recognition test or semantic grading is claimed.
- Apartment Search: real-wheel commands travel through both doorways, reject inspection of the red distractor, and complete a stationary inspection of the yellow cube. Pixel checks confirm the actual head image contains the yellow target. Physics rays verify initial wall occlusion and reject a nearby viewpoint behind the outer wall. Scoring checks reject excessive distance, occlusion, base/head motion, lack of grounding, and repeated same-time observations; looking away resets dwell. Backend-only target metadata stays out of the public preset and observations.
- Apartment browser checks load/reset the fifth preset, autofill its goal and 100-turn limit, and render nonblank desktop/mobile views. The mobile completion-display test uses an explicitly recorded manual placement followed by a physical wait; route feasibility is covered separately by wheel commands. Apartment and target-camera screenshots were inspected.
- Park in the Bay: real wheel commands move the base and wheels completely inside the green zone, then brake to a measured rest on the floor. The browser displays completion and clears it on reset.
- Tidy the Cube: real Cartesian/gripper commands lift the red cube, carry it to the blue floor zone, and release it. Both left- and right-arm placements are exercised in the sorting scene.
- Color Sort: a scripted sequence completes both cubes within a single physics episode; the first placement remains correct after the second.
- Remember and Recharge: a scripted wheel-control route goes around the screen, completes the survey, receives a low-battery sensor reading, returns to the original charger, and reaches at least 90% via bounded waits. A physical ray check confirms direct charger visibility is obstructed. Zero charge interrupts/rejects motion; reset restores full battery and all mission stages.
- Charging tests reject initial camping, charging while moving, partial dock occupancy, and the wrong zone. Repeated observations do not charge the battery. The wait tool is duration-bounded, deduplicated, and respects Stop.
- The text-model memory test verifies the first actual image/sensor observation remains in the eighth request after rolling context eviction. Neither charger coordinates nor evaluator state is added; current feedback remains distinct and last. Battery payloads contain only percentage, low, and charging fields.
- Evaluator tests reject pushing without a recorded lift, hovering, partial containment, nonzero motion above settling thresholds, and swapped colors. Success requires actual floor contact; conservative collision AABB height alone is not used to infer grounding.
- Valid lift evidence includes opposing physical finger contacts after the optional grasp-assist constraint releases. Score checks do not depend solely on that approximation.
- API tests verify six public options, scene/goal/progress replacement, reset of the selected challenge, model-configuration preservation, same-origin mutation protection, and rejection of stale commands after changing tasks during pending inference.
- Voice and text-model boundary tests confirm public goals are available without leaking scene object names, coordinates, or evaluator progress into observations/session instructions.
- Browser tests load each distinct scene, verify goal autofill, checklist counts, head image/canvas output, reset behavior, and mobile fit. Desktop/mobile screenshots were inspected; parking uses a wider initial spectator view.
- These are curated, repeatable scenes with physics-based live checks, not an autonomous-model benchmark. No paid model run was made to establish challenge success. Charging has a timed survey stage and apartment search has a timed visual inspection; generalized dwell scoring, randomized seeds, spherical objects, and persisted results remain out of scope.

## Realtime Voice Evidence

- The voice target is now `gpt-realtime-2` (public preview) with `reasoning.effort=low`. Tests verify the outgoing session update omits unsupported `truncation`, public configuration names the intended model without inventing a deployment, and commentary/final-answer messages are retained without private reasoning. The focused Realtime/API suite passed 20 tests; both voice browser tests and the production build passed after this update. This is protocol/fixture validation, not a live Realtime 2 model result.
- The Foundry GA WebSocket adapter is validated against a real local WebSocket server for JSON send/receive. API-key headers stay server-side; request URLs use the GA resource endpoint and deployment query parameter. Entra uses `https://ai.azure.com/.default`.
- Scripted Realtime events plus real PyBullet verify microphone packets, manual commit, camera/sensor injection, validated wheel movement, tool-result continuation, output PCM audio, and user/robot transcript events.
- Invalid, unknown, multiple, or refused tool requests cannot cause motion. After the model calls stop, a subsequent unsolicited tool call is rejected even if the model ignores `tool_choice=none`.
- Explicit microphone activation is required before audio packets are accepted. Pending inference is canceled on disconnect, and disconnect during motion brakes before a deliberately delayed cloud socket close completes. Global Stop releases exclusive voice ownership.
- Edge browser tests use an oscillator-backed synthetic MediaStream, never the user's microphone. They verify no microphone request before clicking, capture to PCM, real scripted-tool motion, scheduled audio playback, track release after sending, End voice, global Stop while recording, and denied-permission cleanup.
- Desktop/mobile voice screenshots were inspected. Captions, connection inputs, microphone/send buttons, and End voice fit at 1440 and 390 pixels. The audio worklet did not initialize within the bound on this machine; the native ScriptProcessor fallback passed the end-to-end test. Real physical audio quality and the preferred worklet path still need device validation.
- WebSockets 17.1 was already installed and locked. The project now declares it directly, and `uv lock --offline` succeeds. Downloads for an older optional OpenAI Realtime extra failed TLS, so the implementation uses the installed WebSockets library with Foundry's documented GA protocol. No certificate verification or registry policy was bypassed.
- No live Realtime endpoint, speech-recognition accuracy, conversational latency, voice quality, or speech-understanding model result is claimed. No paid Realtime deployment was created or invoked.

## Exchange Feed Evidence

- Recorded sensor JSON and retained PNG bytes match the exact inputs passed to the model adapter. Visible response text, requested arguments, and tool results are recorded in execution order, including results of interrupted motion.
- Pending inference exposes its submitted input before an output exists; cancellation creates an interruption outcome without fabricating an LLM reply.
- Private/encrypted reasoning and credential markers are excluded from feed payloads. Malformed nonfinite arguments remain safely serialized text in the visible model output.
- Incremental trace cursors and the 200-event retention bound are tested. Input images remain available after 17 later observations evict them from the simulator's observation-frame cache; episode reset invalidates old trace image URLs. Live operator previews have a separate cache.
- Browser tests verify chronological order, decoded input images, sensor/result expansion, input/LLM/tool filters, follow-latest scrolling, inspection without autoscrolling, and reset clearing. A populated mobile feed fits the viewport; desktop/mobile captures were inspected.
- These feed tests use scripted inference with real physics. No new live model request or cloud telemetry export was made for this feature. Feed data is local and session-scoped, not persisted to disk.

## LLM Control Evidence

- Built-in profiles now bind Luna to `gpt-5.6-luna` (default) and Nano to `gpt-5.4-nano`. The legacy GPT-5.2 setting remains an additional profile. Eight focused profile tests and two API checks passed for alias handling, duplicate prevention, custom JSON precedence, actual adapter selection, and reset persistence. Three browser checks passed for Luna's initial selection, Nano's scripted run, and existing custom-profile controls. Build and editor diagnostics pass. The full-suite results above include these configuration changes.
- Custom-list regression: an explicit list with unconfigured Luna and configured GPT-5.2 still selects its first configured profile. Browser tests retain precise missing-endpoint versus missing-deployment messages without falsely reporting an authentication failure. Built-in bindings and the configured indicator do not establish live deployment access; no new Luna or Nano inference or cloud deployment was performed.
- Scripted model responses drive real physics through the canonical tool schema and existing worker; no mock replaces robot mechanics.
- Tests cover feedback pacing, updates during a pending wait, image/sensor allowlisting, tool-call/output continuation, malformed and nonfinite arguments, unknown tools, multiple actions, duplicate IDs, refusals/filtering, inference timeout, repeated stop during motion, and client cleanup.
- HTTP tests cover configuration validation, model selection, manual/LLM ownership, reset invalidation, last-operator disconnect, and atomic publication of ready workers during resets.
- OpenAI SDK requests were intercepted in a test to verify the actual payload includes the chosen deployment, `reasoning.effort=low`, image input, eight function tools, no parallel tool calls, and `store=false`.
- `backend/.env` is loaded ahead of root `.env` without overriding process values. The supplied project endpoint and `deployment_name` aliases are supported. Tests isolate themselves from the user's connection and credentials.
- A real, no-motion probe used the configured `GPT-5.2` deployment with a live robot image and sensor payload: response `completed`, tool `observe`, 2197 input tokens and 27 output tokens. No tool from this response was executed. This verifies the supplied connection, image input, low reasoning, and function calling; it does not establish task performance or Luna availability.
- Project-level standard `/models` listing returned 404, and the Azure resource lookup did not find the account in its current context. A successful Responses call is the connection evidence; no cloud resource was created or changed.

## Installation Findings

- The initial Windows PyPI source-build route was unusable without a C++ build toolchain.
- Windows binary installation succeeded through conda-forge after selecting OpenBLAS. This avoided the failing MKL/ICU/XML dependency chain.
- Launch the environment's Python executable directly. A fresh shell's `micromamba run` silently failed on this machine, although the installed interpreter and native physics imports worked.
- Node.js 24.12.0 and npm 11.6.2 are installed.
- Earlier requests to `https://packagefeedproxy.microsoft.io/npm/` timed out, and offline caches lacked required packages. Access is now restored; installation through that approved feed succeeded.
- The generated frontend lockfile contains 133 HTTPS tarball URLs on `ms-feed-25.pkgs.visualstudio.com`. A fresh metadata request through the CFS proxy confirmed that internal tarball host. No blocked public registry was used.
- Updated Vite from 7.1.3 to 7.3.6 and Playwright from 1.55.0 to 1.55.1 to resolve the reported dependency advisories. The subsequent install audit reports **0 vulnerabilities**.

## Not Verified or Not Implemented

- Browser-driven end-to-end arm pickup/release, additional viewport sizes, browsers other than Edge, and prolonged disconnected/reconnected sessions are not yet covered by the frontend suite. Both-arm pickup/release is covered by real backend physics tests.
- A configured speech-to-speech Realtime deployment is required for real voice use. Full-duplex interruption/barge-in, physical microphone/speaker quality, and continuously advancing physics during model thinking are not implemented or verified by synthetic voice tests.
- Luna inference and full autonomous navigation/pickup/release runs remain unverified. Ollama is implemented as described above; other local OpenAI-compatible and Foundry Local adapters are not implemented. Scripted inference exists only in tests, not as a selectable production model.
- JSON argument repair, continuous real-time physics during inference, automated model-capability discovery, persistent memory, cost estimates, and prompt-injection robustness evaluation are not implemented. The instruction boundary and tool validation exist, but are not a robustness guarantee.
- The full benchmark challenge specification, generalized timed dwell scoring, held-out/randomized seeds, and challenge import/export are not implemented. Six curated presets have private manipulation, navigation, staged energy, and visual-search checks. Autonomous memory, apartment-search, and kitchen/bathroom recognition performance remain unmeasured; room descriptions are not automatically graded.
- SQLite/local artifact recording, OpenTelemetry, retention limits, replay, result comparisons, seed batches, downloadable results, and saved end-to-end demonstration runs: not implemented.
- Linux startup, optional Azure deployment, authenticated production serving, and cross-platform reproducibility: not verified.

## Resume Point

The frontend installation blocker is resolved. Reproduce the mechanical bench gates with:

```powershell
npm --prefix frontend ci --registry=https://packagefeedproxy.microsoft.io/npm/
npm --prefix frontend run build
./.runtime/env/python.exe -m pytest -q
./start.ps1 -SkipBuild
```

Run `npm --prefix frontend test` with port 8001 free; it manages its own isolated test server. The local UI on port 8000 defaults to `gpt-5.6-luna` and offers `gpt-5.4-nano` for testing, retaining legacy deployments such as GPT-5.2 as alternatives. Voice requires `FOUNDRY_REALTIME_ENDPOINT` and `FOUNDRY_REALTIME_DEPLOYMENT`, or equivalent UI connection settings. Remaining gates include a live Realtime conversation, verified Luna/Nano connections, and an observed autonomous task run. Preserve the robot-observation boundary; do not report autonomous results until they have actually been executed and recorded.